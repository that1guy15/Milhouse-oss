# Milhouse OSS Implementation Status

This file is the durable execution ledger for the
[authoritative implementation plan](implementation-plan.md). It records gate
status and evidence; it does not amend the plan.

## Current snapshot

| Field | Value |
|---|---|
| Snapshot date | 2026-08-02 |
| Product phase | **Pre-alpha; not released and not ready for production use** |
| Plan | Version 1.0, approved for implementation by the owner on 2026-07-18 |
| Target release | Milhouse OSS 1.0 |
| Working branch | `codex/w03-g03-fail-record` (this PR is the **docs / change-control correction** that fixes PR #78's own independent FAIL (D08(A), [5154004969](https://github.com/that1guy15/Milhouse-oss/pull/78#issuecomment-5154004969), head `349150e`, 3 P1 + 1 P2): it corrects the gate packet and this ledger so they no longer contradict the live review record, and it **completes owner-approved amendment A07** to the change-control standard of plan §1 — reason, alternatives, compatibility/migration/security impact, revised tests — across the ADR 0007 addendum, the plan §1 register, the plan §4.7 edit, and the ADR index, plus a change-control meta-test (`tests/unit/test_amendment_change_control.py`) that asserts every numbered amendment records its required fields and is registered in the ADR index; it also bumps the `docs-check` external-link bound for the added review-artifact URLs. No spool/audit source is changed here — the code rework is the separate PR #79. **A07 corrected per the owner's 2026-08-01 decisions:** (1) compaction binds the installation's control-plane key ID/epoch (migration 11 `_installation_key`) and loads with `expected_key_id`/epoch + state-root binding, failing closed before any mutation; (2) the reserved `c[0-9a-f]{64}` namespace has no public publish bypass and a legacy committed occupant is AUTO-CONVERGED (rehomed to a non-reserved id), never a wedge; plus a compaction retirement tombstone. Live review chain: **PR #74** ([5148867847](https://github.com/that1guy15/Milhouse-oss/pull/74#issuecomment-5148867847), head `a2e71b7d`) is a DISTINCT review whose retention-crash-re-adopts P1 applies to unchanged code; **PR #76** ([5151585903](https://github.com/that1guy15/Milhouse-oss/pull/76#issuecomment-5151585903), head `f9ce189`, tree `fb372e2` = the `91b97b0` squash) is the exact-tree review that FAILed with 3 P1 + 1 P2 over 149 tests; **PR #77** posted no review (its "ready" packet was a later overstatement); **PR #79** round-1 head `1b19a91` FAILed 4 P1 ([5152583787](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5152583787)) and round-2 head `9f0947a` FAILed 4 P1 (D08(B), [5154152313](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5154152313)). W03 and G03 stay **In progress** pending each remediation SHA's own fresh independent full-SHA review. See D07, D08, and A07) |
| Git state | Protected `main` is `c7390ed023789f9839d6a5a2858fc73b223cfca5` (PR #77 packet/ledger-finalization squash, 2026-08-01, docs-only); the earlier `91b97b03d8b7b503a895697ac10a0931b9a229c0` was the PR #76 remediation squash (post-merge Required CI green [run 30702416917]; squash tree `fb372e2` == reviewed PR-head tree, `src/` byte-identical to gate-tested `e452756`) whose exact tree independently FAILED review (D07); the prior `3c2e14faf991672bd953fac50f41d6ef14bdf3a5` was the PR #75 ledger-integrity correction squash, 2026-08-01; the prior `ced5513e1616a587b3a9079321967bac49971e3e` was the slice-7 harness + candidate G03 evidence PR #72 squash; slice-5c compaction PR #69 `eb8defd`; D05 commits: PR #67 `364d27c`, #65 `da9b9cd`, ledger PRs #64 `5bf2a95`/#66 `bf4f8d1`/#68 `ba1903a`/#71 `411d795`). Slice 5's code landed across PR #56 (slice 4, `b8fff87`), #58 (5a `spool verify`, `f37be0d`), #60 (5b part 1 audit + preview, `5841eec`), and #62 (5b part 2 `retention_apply`, `a7e0f96`). **An independent gate review on 2026-07-30 failed all six PRs #58–#63 and reproduced four unresolved P1 contracts on those merged trees (recorded as D05):** delivery-integrity (`deliver_segment` can certify the wrong data), audit-privacy (the `_audit` API persists arbitrary raw text), the privacy-expiry hard bound (a `_cursors` foreign key blocks pruning a privacy-expired cursor-referenced segment), and false review/completion evidence in this ledger. The earlier "independent exact-head PASS/no-P0-P1" notes for #58/#60/#62 were **same-session in-process adversarial subagent checks, not independent public GitHub reviews** (GitHub holds no reviews/comments on #58–#63), and they missed these P1s — that language is **withdrawn**. **Slice 5b is not complete.** G02 remains accepted; G03 stays **In progress** and no further W03 status may advance and slice 5c may not merge until the P1s are remediated with adversarial regression evidence and a fresh full-SHA review. That first remediation merged as PR #65 (`da9b9cd`), but a fresh independent gate review on 2026-07-30 **FAILED PR #65's exact head with four P1s and one P2** (recorded under D05): a delivery frame-snapshot TOCTOU, delivery not fenced against privacy-expiry retention, an audit resource that admits secret-shaped strings, a post-unlink orphan misclassification, and this ledger's disproven "remediated" claim. **PR #67 (`364d27c`) then re-remediated all four P1s and the P2 with adversarial regression evidence and green CI, and was merged on 2026-07-30 on the owner's direct approval (owner acceptance under their external mutation authority) — NOT an independent public full-SHA review, which remains advisable before release-gate review (E05).** The slice-5b remediation is accepted. **Slice 5c (audited compaction) then landed via PR #69 (`eb8defd`): its first head failed an independent review with three P1s — a predictable, caller-space successor id could retire a mixed segment against a foreign segment and lose live records; a crash + reconciliation regressed a delivered checkpoint to pending; and the audit stored a public unsalted hash rather than a keyed pseudonym — all three remediated on head `c716607` and merged 2026-07-31 on the owner's direct approval (owner acceptance, NOT an independent public review). Per plan §4.7 the audit identifier derivative is now OMITTED until `init` (W06) wires the keyed HMAC pseudonym; migration 8 (schema 8) clears any legacy unsalted rows.** **Slice 7 (the crash/concurrency/failure-injection harness + candidate G03 evidence packet) then landed via PR #72 (`ced5513`): its head failed an independent review with three P1s and one P2 in the compaction/audit code the harness exercises — a foreign-occupied successor id could strand privacy-expired data (attempted fix: compaction probes a bounded deterministic alternative sequence), the audit boundary persisted unvalidated pseudonymizer output (attempted fix: canonical keyed-token grammar validation), a compacted segment mislabelled a reconstructed midnight as `committed` (fixed: `origin=reconciled`), and the compaction audit lineage's keyed identifiers were recorded as deferred to `init` (W06) — merged on head `9128fad` on the owner's direct approval (owner acceptance, NOT an independent public review).** **A fresh independent gate review on 2026-07-31 of the integrated head `main` `b2822f3` (tree `6d89f6b`; the merged tree is identical to PR #72 head `9128fad`) returned verdict FAIL with four P1 blockers and one P2 (recorded as D06), disproving those remediations:** the bounded collision probe still strands expired data (a finite predictable id set plus a `collision_bound` terminal); grammar validation is not proof of keyed derivation (an overridable `fingerprint()` can persist caller-controlled shaped input); the compaction lineage cannot be deferred to W06 by an evidence note (plan §4.7 locks it to W03, and W06 sits downstream of G05 while W05 depends on G03 — a dependency cycle needing an owner decision); this ledger's "remediated / all seven slices complete / candidate" claim was itself disproven; and the G03 packet is not bound to final exact-head evidence. **The "all seven W03 slices complete / G03 candidate" language is WITHDRAWN. Slice 5c and slice 7 are NOT complete; W03 and G03 return to In progress** pending remediation of the four P1s on a new protected-`main` SHA, a fresh independent full-SHA review, the pending owner decision on compaction-lineage ownership, supported-host physical-crash evidence (E06), and the packet refresh. **Remediation candidate in flight (2026-08-01): PR #75 (ledger correction) MERGED → `3c2e14f`; the owner decided to wire keyed lineage into W03 now; PR #76 (branch `codex/w03-g03-remediation`) carries the three P1 code fixes + the packet refresh — recorded as remediated-pending-independent-review, NOT accepted (see D06). Two independent reviews then FAILED that path (D07), a first code rework (PR #79 head `1b19a91`) FAILED (D07(D)), and round 2 FAILED both PRs (D08): PR #78 head `349150e` (docs/A07) and PR #79 head `9f0947a` (code). The corrected-A07 round-3 rework lands PR #78 first, then rebases PR #79; `main` is unchanged at `c7390ed`** |
| Public source baseline | `that1guy15/Milhouse-oss@fb81a7faf2c101e8bb3f08ef9120d82c2b20600b` |
| Private reference baseline | `that1guy15/milhouse@18ee9514ee11413812fde8fe361405b3686e025f` |
| External mutation authority | Owner authorized build-branch pushes, pull requests, and merges to `main` after required checks on 2026-07-19. On 2026-07-22 the owner separately authorized maintainer-authored engineering-journal posts in GitHub Discussions when they use only merged public evidence and clearly remain pre-release. On 2026-07-29 the owner separately authorized creating and saving an unpublished draft in the authenticated personal Substack for every completed learning blog derived from those Discussion posts about current work or features. Substack publication, scheduling, email delivery, account changes, tags, package publication, release/availability announcements, live-provider calls, and unrelated external mutation remain separately controlled |
| Highest passed gate | G02 |
| Active package | W03 — SQLite state, durable spool, replay, and retention; **In progress**. G02 was accepted by the owner on 2026-07-24. A06 resolves the filesystem-containment contract stop condition; the five D04 P1 findings on PR #45's merged tree are corrected and merged by PR #54 (`1855d36`) after an independent exact-head PASS review. Slices 1–4 are merged: the secured SQLite control plane, self-describing spool segments and durable ledger commit, mandatory reconciliation and quarantine/recovery, the structured-log file surface, and (PR #56, `b8fff87`) source cursors, per-rule/version derivation checkpoints, the exporter delivery protocol, and deterministic idempotent replay. Slice 5's code merged across 5a `spool verify` (PR #58, `f37be0d`), 5b part 1 `_audit`/`retention_preview` (PR #60, `5841eec`), and 5b part 2 `retention_apply` (PR #62, `a7e0f96`), but an independent gate review (2026-07-30, **D05**) reproduced four P1 contracts on those merged trees, so **slice 5b is not complete** and its earlier PASS/complete claims are withdrawn. A first remediation merged (PR #65, `da9b9cd`), but an independent re-review on 2026-07-30 **FAILED it with four P1s and one P2** (D05): a delivery frame-snapshot TOCTOU, delivery not fenced against privacy-expiry retention, an audit resource that admits secret-shaped strings, a post-unlink orphan misclassification, and this ledger's disproven "remediated" claim. PR #67 (`364d27c`) re-remediated all four P1s and the P2 with adversarial evidence and green CI and was merged on 2026-07-30 on the owner's direct approval (owner acceptance, **not** an independent public full-SHA review — advisable before release-gate review E05). The slice-5b remediation is accepted. **Slice 5c (audited compaction, PR #69 → `eb8defd`) then landed** after its first head failed an independent review with three P1s (successor-id collision → live-record loss; a delivered checkpoint regressed to pending across crash + reconcile; a public unsalted audit hash instead of a keyed pseudonym), all remediated on head `c716607` and merged 2026-07-31 on the owner's direct approval (owner acceptance, not an independent public review). Per plan §4.7 the audit identifier derivative is now OMITTED until `init` (W06) wires the keyed HMAC pseudonym; migration 8 (schema 8) clears any legacy unsalted rows. **Slice 7 (the crash/concurrency/failure-injection harness + candidate `docs/gate-evidence/G03.md`) then landed via PR #72 (`ced5513`)** after its head failed an independent review with three P1s and one P2, merged 2026-07-31 on the owner's direct approval (owner acceptance, not an independent public review). **A fresh independent gate review on 2026-07-31 of the integrated head `main` `b2822f3` (tree `6d89f6b`; equivalently PR #72 head `9128fad`) returned verdict FAIL with four P1 blockers and one P2 (recorded as D06):** the bounded compaction collision *probe* still strands privacy-expired data (needs a reserved successor namespace or an unpredictable durable reservation); grammar validation of an overridable `fingerprint()` is not proof of keyed derivation (needs the exact concrete `Pseudonymizer`); the compaction audit lineage cannot be deferred to W06 by an evidence note (plan §4.7 locks it to W03; W06 sits downstream of G05 while W05 depends on G03 — an owner decision is pending); the ledger's "remediated / all slices complete" claim was disproven; and the packet is not bound to final exact-head evidence. **The "all seven W03 slices complete / G03 candidate" language is WITHDRAWN — slice 5c and slice 7 are NOT complete and G03 stays In progress**, pending remediation of the four P1s on a new protected-`main` SHA, a fresh independent full-SHA review, the pending owner decision on compaction-lineage ownership, supported-host physical-crash evidence (E06), and the packet refresh. **Remediation MERGED (2026-08-01): the ledger correction landed as PR #75 → protected `main` `3c2e14f`; the owner decided to wire keyed lineage into W03 now; and the code fixes landed as PR #76 → protected `main` `91b97b0` (post-merge Required CI green) — reserve the `c[0-9a-f]{64}` successor namespace at the commit ingress (removing the bounded probe), require the exact concrete `Pseudonymizer` type at the audit boundary, and thread the installation `Pseudonymizer` through compaction — plus the packet refresh (finding #5). G03 was then marked ready for review.** **However, TWO independent reviews then disproved that (D07): the PR #76 exact-head review ([5151585903](https://github.com/that1guy15/Milhouse-oss/pull/76#issuecomment-5151585903), 149 tests) FAILED with 3 P1 (same-survivor/different-exporter collision still strands; keyed lineage optional/unwired → NULL `_audit`; audit omits the mandated old/new content/file hashes) + 1 P2, and the PR #74 review ([5148867847](https://github.com/that1guy15/Milhouse-oss/pull/74#issuecomment-5148867847), 8 tests) FAILED with a retention-crash-re-adopts P1 among others. PR #77 posted no review; its "ready" packet was a later overstatement. A code-remediation attempt (PR #79, head `1b19a91`) then FAILED its own independent review with four P1s ([5152583787](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5152583787)). **Round 2 also FAILED both PRs (D08): the docs/A07 correction PR #78 head `349150e` FAILED (3 P1 + 1 P2, [5154004969](https://github.com/that1guy15/Milhouse-oss/pull/78#issuecomment-5154004969)) and the code rework PR #79 head `9f0947a` FAILED (4 P1, [5154152313](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5154152313)).** The owner then decided (Q1) to wire full installation key-ID/epoch binding into W03 now (control-plane `_installation_key`, migration 11) and (Q2) to auto-converge legacy reserved-namespace occupants by rehoming them; **owner-approved amendment A07 is corrected to that contract** (ADR 0007 addendum + plan §1 register + §4.7 + ADR index). The "ready / findings addressed" language stays WITHDRAWN; G03 and W03 stay In progress. Sequence: PR #78 (docs/A07/ledger) lands on protected `main` first behind a fresh independent full-SHA PASS, then PR #79 rebases onto it; no acceptance until each remediation SHA passes its own fresh independent full-SHA review with no P0/P1, plus E06 supported-host physical-crash evidence and owner acceptance. See D07, D08, and A07.** W04 remains dependency-ready in parallel |

The audited public baseline is the source scaffold. The private baseline is a
read-only behavior and algorithm donor. Its history, configuration, telemetry,
generated data, private fixtures, and private documentation must never enter
this repository. See [provenance.md](provenance.md).

Read-only GitHub API evidence captured 2026-07-18: the repository is public,
unarchived, and uses protected `main`; Issues and Discussions are enabled;
secret scanning, push protection, and Dependabot security updates are enabled;
Private Vulnerability Reporting is enabled as of 2026-07-19. GitHub correctly refused a sole
administrator self-report, then the documented administrator-side synthetic draft was created,
read back, and closed without publication, CVE, or private fork. On 2026-07-19 the owner authorized
the solo-maintainer path and engineering changed the review sub-rule to zero approvals, no required
CODEOWNER approval, and no last-push approval. After the W01 aggregate first passed, the temporary
`test`/`gitleaks` contexts were replaced by the single strict GitHub Actions `required-ci` context.
Pull requests, stale-review dismissal, conversation resolution, administrator enforcement, no
force-push, and no deletion remain. An intentional W01 failure subsequently proved the required
aggregate blocks merge.

## Status and evidence rules

The only package states are:

- **Pending** — dependencies or deliverables are incomplete; work may not be
  claimed as accepted.
- **In progress** — implementation is active, but the gate has not passed.
- **Externally pending** — all in-scope engineering evidence is ready, but a
  named owner/reviewer/host/elapsed-time action is still required.
- **Blocked** — a plan stop condition applies. Record the exact condition and
  the needed amendment or authority.
- **Passed** — every gate assertion has current, reviewable evidence and no
  mandatory check is skipped.

A passing entry must record all of the following in this file or in a linked,
immutable evidence record:

1. the exact commit and installed artifact hash where applicable;
2. commands and exit results, including required tests and scans;
3. the operating system, Python, Docker, and ClickHouse versions where relevant;
4. migration/schema/config/API versions exercised;
5. failure-injection or recovery evidence required by the gate;
6. any independent review and owner authorization;
7. open defects and their severity/disposition; and
8. the date and accountable reviewer who accepted the result.

Skipped tests, fixture-only provider behavior presented as live compatibility,
placeholder commands, and unverified external settings are not passing
evidence. A later regression returns the affected gate to **In progress**.

## Work-package ledger

| Package | Depends on | State | Gate evidence required before `Passed` | External owner/reviewer action |
|---|---|---|---|---|
| W00 — authority, governance, ADRs | Owner plan approval | **Passed** | PR #1 merged as signed squash commit `79b9fdca3c567b1de48a3136fbe4ba0dd981926a`; reviewed head tree matched protected `main`; post-merge `test` and `gitleaks` passed | E01 and E02 complete for G00; third-party PVR delivery remains E01/E05 at G17 |
| W01 — package and quality toolchain | G00 | **Passed** | [G01 evidence](gate-evidence/G01.md): exact candidate and remediation trees passed local gates, 18-check hosted PR runs, three independent reviews, signed protected merges, exact-tree equality, post-merge required CI, and corrected default-branch updater execution at commit `333c051ea1018a624715b98bf5dad7c885bdeca5` | G01 accepted by engineering on 2026-07-19 under the owner-authorized solo-maintainer path; aggregate `required-ci` and protection controls remain active |
| W02 — domain, config, identity, privacy | G01 | **Passed** | Merged PRs #8-#20 provide deterministic bytes/IDs, strict config/schema/CLI foundations, canonical record envelopes, keyed pseudonyms, safe URL/path/evidence handling, layered redaction and allowlists, secure runtime paths and secret loading, pseudonym-key material creation/recovery, injected clocks and bounded durations, stable coded errors, sink-agnostic structured events, the fail-closed common egress matrix, adversarial domain/redaction boundaries, and metadata-only exact third-party plugin validation. PR #21 added a public-safe immutable identity corpus, independent-process recomputation under varied mapping order, hash seed, timezone, and locale, aggregate-required macOS/Python 3.11 and 3.14 coverage, and the omitted-correlation JSON parsing repair. Its reviewed source and protected squash trees are identical, but malformed squash-body line breaks made the protected commit's intended trailer non-parseable. [Immediate incident recovery](gate-evidence/PR21-DCO.md) is complete through signed protected PR #22 and green post-merge Required CI; the original squash remains noncompliant and its exact historical disposition is recorded by owner-approved amendment A03 (2026-07-22, ADR 0017). Plan amendments A02 (persisted local structured-log contract, plan section 4.15, ratified by ADR 0016) and A03 (exact D01 historical DCO disposition, ratified by ADR 0017) were owner-approved on 2026-07-22; the `local_log` egress surface (PR #27), CanonicalJSONV1 event-line wire and encoder (PR #28), golden/determinism corpus (PR #29), stream sink (PR #30), the G02 evidence packet (PR #31), and the completed, hardened, and schema-locked wire — header/trailer/digest, hostile-failure normalization, and amendment A05 exact v1 schema (PR #33, `fad1479`) — are merged, each with independent report-only review and green Required CI. Owner-approved plan amendment A04 / defect D02 (2026-07-23) re-scopes the G02 assertions to the W02-owned redaction, rendering, structured-error, structured-event, and `local_log` wire and stream-sink primitives, deferring the concrete structured-log file, CLI/stderr/diagnostics, and generated-report surfaces to G03, G06, and G09. the A04/D02 gate-scope correction is merged at `92d99d1` (PR #34), and PR #35 corrects the acceptance evidence and lands the deferred G03/G06/G09/G16 `local_log` obligations. [G02 evidence](gate-evidence/G02.md) records the corrected W02-owned assertions satisfied and the 95% critical-branch floor met across 36 enforced files. **G02 was accepted by the owner on 2026-07-24**, bound to protected `main` `90f8e4b` with green post-merge Required CI (run 30064408885); W03 and W04 are unblocked | G02 accepted by the owner 2026-07-24 at `90f8e4b`; W03 (SQLite/spool/replay/retention) is the next package |
| W03 — SQLite, spool, replay, retention | G02 | **In progress** | Slices 1–3d merged in PRs #38–#44 through protected `main` `075d986`: secured SQLite control state, ordered migrations, fenced leases, the global commit barrier, self-describing spool segments, atomic writer and durable ledger commit, secure bounded reads, mandatory reconciliation, and quarantine/recovery hardening. PR #44 exact head passed independent review with no actionable findings and green hosted checks before the verified merge. PR #45's structured-log persistence tree merged at protected `main` `454736f` without an exact-head PASS; a post-merge exact-head review found five P1 defects (D04), all corrected and merged by PR #54 (`1855d36`) after an independent exact-head PASS review and green Required CI. Slice 4 merged by PR #56 (`b8fff87`) after an independent pre-merge exact-head PASS: source cursors advanced only against committed segments (step 9), per-rule/version derivation checkpoints (step 10), the exporter delivery protocol with a terminal-`delivered` state machine (steps 11–12), and deterministic idempotent replay that re-reads and cross-checks each durable segment against its ledger row before reprocessing. Concrete exporters (W04), derivers (W08/W11), and the per-record query index (W09) remain deferred. Slice 5 (retention + audit + compaction) code landed as 5a `spool verify` (PR #58, `f37be0d`), 5b part 1 `_audit` + `retention_preview` (PR #60, `5841eec`), and 5b part 2 `retention_apply` + the secure no-follow removal primitive (PR #62, `a7e0f96`). The pre-merge "independent exact-head PASS/no-P0-P1" notes on those PRs were **in-process same-session adversarial subagent checks, not independent public GitHub reviews**, and an independent gate review on 2026-07-30 (**D05**) subsequently reproduced four P1 contracts on the merged trees: `deliver_segment` can record a terminal `delivered` while forwarding frames that need not belong to the segment; the `_audit` API persists arbitrary raw text; the non-cascading `_cursors.batch_id` foreign key makes a privacy-expired cursor-referenced segment un-prunable (retention silently skips the integrity error); and this ledger recorded overstated review/completion evidence. **Slice 5b is therefore not complete** and its PASS/complete language is withdrawn; the P1s (plus P2 shadow-spool verification, P2 post-unlink orphan misclassification, and P2 the removal primitive not enforcing exact `0600`/ACL) are being remediated on `main` with adversarial regression evidence and a fresh full-SHA review, before any slice-5c or further W03 advancement. G03 still requires every durable-write kill point, concurrency, torn-tail recovery, persisted health and verification, replay of 10,000 records twice, retention/privacy-expiry behavior, and supported-host physical crash evidence | Independent full-SHA review after each remediation; access to a supported local filesystem/host if not available to engineering |
| W04 — ClickHouse and recovery | G02; G04b also requires G03 | Pending | G04a auth/migrations/checksums; G04b idempotent delivery, 24-hour simulated outage drain, and verified native restore | Docker-capable supported host if not available to engineering |
| W05 — runtime/canary vertical slice | G03 and G04 | Pending | Full canary-to-query slice; ClickHouse outage behavior; exact-once logical drain; alert transition cases | None beyond review |
| W06 — initialization, CLI, demo | G05 | Pending | Nonexpert clean-host flow; idempotent init; credential-free spool demo; health exit codes; installed/source parity | E06 clean-host evidence where engineering lacks the host |
| W07 — file/authenticated ingestion | G05 | Pending | Incremental outbox behavior; acknowledged rotation; P1 detection for unread loss; bounded/replay-safe receiver; offline CI | Owner acknowledgement is required before any non-loopback receiver test |
| W08 — feedback and verification | G07 | Pending | Complete transition matrix; claim cannot verify; idempotent curation; SQLite/ClickHouse parity; verified/regressed synthetic stories | None beyond review |
| W09 — query, reports, repo briefs | G08 | Pending | Metric-window correctness; path/symlink security; bounded/degraded reports; deterministic safe rendering | A configured application repository may be supplied for a final sandbox smoke; fixtures remain the CI basis |
| W10 — MCP | G09 | Pending | Official-client conformance; read-only default; bounded/privacy-filtered results; dual write enablement and idempotent audit | Local client smoke may require owner-provided supported client environment |
| W11 — `/doh` postmortems | G08 and G10 | Pending | Neutral evidence model; all required contributors in scope; untrusted rendering; missing-evidence and scenario tests | None beyond review |
| W12 — provider collectors | G05 | Pending | Per-provider fixture, pagination/rate/cursor/drift/privacy tests; no overlap inflation; least-privilege docs | E04 owner-authorized sandbox smoke is required before each adapter is labelled supported; otherwise it remains experimental |
| W13 — agent summaries/traces | G05 and G08 | Pending | Disabled default; idempotent incremental parse; visible drift; planted prompt/PII/secret absent from every persistence/egress surface | E04 owner-authorized format smoke is needed for a current supported-label claim |
| W14 — notifications/action sinks | G09 and G12 | Pending | Disabled default; redacted dry run; idempotent retry; safe Markdown; no secret/audit leakage; issue closure cannot verify | E04 separately authorizes each sandbox Telegram/GitHub write smoke; no production destination is assumed |
| W15 — scheduler/services | G06, G08, G09, G11, G12, G13, G14 | Pending | Lease exclusion; restart/replay; independent failure containment; freshness; generic service-template clean-host tests | E06 macOS/Ubuntu service-template hosts where unavailable to engineering |
| W16 — import/recovery/lifecycle | G03, G04, G09, G15 | Pending | Read-only/idempotent legacy import; clean-host point-in-time restore; upgrade/rollback; explicit destructive confirmation; RPO/RTO | E06 clean-host restore hosts; any real private legacy source remains owner-controlled and is never required by CI |
| W17 — docs/release hardening | G10–G16 | Pending | All doc/config/skill checks; exact artifact contents; installed workflows; scans; SBOM/provenance; no P0/P1 | E01, E03, E05, E06, and E08 protected-release settings/approvals; revisit required human review when a second maintainer exists |
| W18 — performance/soak/RC | G17 | Pending | Fixed-seed benchmarks; platform/recovery drills; 7/14/7-day soaks; two independent clean hosts; exact RC artifacts; no P0/P1 | E06 hosts, E07 elapsed environment, E05 review, and E08 RC/publish decisions |

“Pending” above means “not accepted”; it does not prohibit dependency-safe
preparatory work allowed by the plan. Dependencies and gates remain normative.

## W00 evidence checklist

| W00 item | Status at this snapshot | Evidence or remaining work |
|---|---|---|
| Owner approved plan 1.0 | Complete | Build instruction received 2026-07-18; external mutation remains separately controlled |
| Workspace uses audited public baseline without private history | Complete | Local branch parent and `origin/main` both resolve to `fb81a7faf2c101e8bb3f08ef9120d82c2b20600b`; only public history is present and private history was never imported |
| Branch `codex/build-milhouse-v1` | Complete | `git branch --show-current` observed locally on 2026-07-18 |
| Plan retained and status ledger created | Complete | [implementation-plan.md](implementation-plan.md) and this file |
| Locked decisions ratified by ADRs | Complete | `docs/adr/README.md` indexes 14 plan ratifications, accepted process ADR 0015, contract amendment ADR 0016, disposition ADR 0017, and the owner-approved A06 addendum to ADR 0004; local link and plan-anchor validation passed |
| README is truthful pre-alpha | Complete | README labels the current command/config/deployment as scaffold and gates every planned capability |
| Privacy contract | Complete as a W00 document | [../PRIVACY.md](../PRIVACY.md); implementation guarantees are not claimed operational until their gates pass |
| Threat/data inventory | Complete as a W00 document | [threat-model.md](threat-model.md); implementation controls still require their owning tests |
| Governance and DCO | Complete as a W00 document | [../GOVERNANCE.md](../GOVERNANCE.md); solo-maintainer pull-request/check policy recorded and applied. Automated DCO enforcement is a W01/W17 control |
| Support policy | Complete as a W00 document | [../SUPPORT.md](../SUPPORT.md) |
| Provenance inventory | Complete as a W00 document | [provenance.md](provenance.md); update per adapted donor file |
| Five-skill engineering workflow | Complete | ADR 0015 pins `EveryInc/compound-engineering-plugin@8163a96e86656a89797869ac61905fe4641f81be` as a concept-only MIT reference. Five Milhouse-native skills, aliases, canonical instructions, sanitized evaluations, and strict negative validation merged in signed commit `79b9fdca3c567b1de48a3136fbe4ba0dd981926a`; final review found no P0-P2 and protected post-merge checks passed |
| Private vulnerability path | Complete for G00 | API read-back returned `enabled: true`. The private-report endpoint returned the documented HTTP 403 for the sole repository administrator; GitHub directs administrators to create a draft advisory instead. A clearly marked low-severity synthetic draft was created at 14:41:07Z, read back as private draft, and closed at 14:41:22Z with `published_at: null`, no CVE, no private fork, and zero remaining triage reports. A true third-party delivery smoke is deferred to E05/G17 when an independent reviewer exists |
| Merge/review path | Complete | Owner authorized solo-maintainer merges on 2026-07-19. PR #1 merged through the repository's squash-only policy as signed commit `79b9fdca3c567b1de48a3136fbe4ba0dd981926a`; the reviewed head tree matched protected `main`, and both post-merge checks passed |
| Apache-2.0 ownership/provenance and DCO | Complete for W00 | Apache-2.0 retained; DCO policy and sign-off workflow documented; donor ledger records no private expression imported. Independent release review remains E05 for G17/G18 |
| Stale private-first/duplicate workflow instructions removed | Complete | Canonical workflows remain only under `.github/workflows`; stale `ops/github/workflows` copies/instructions removed; OpenWiki marked optional/noncanonical |

G00 **passed** on 2026-07-19 at protected-main commit
`79b9fdca3c567b1de48a3136fbe4ba0dd981926a` after the plan/ADR index, merge path,
public tree, private-reference boundary, seven supplemental P2 dispositions, and exact-tree hosted
checks were reviewed together.

## W00 validation evidence — 2026-07-18 through 2026-07-19

- The DCO-signed W00 commits through prior evidence head
  `11652eff2ec46f9c895c41a1c1a16aa28eac70b8` are pushed to PR #1.
- Branch `codex/build-milhouse-v1`, `HEAD`, and `origin/main` were verified at
  public baseline `fb81a7faf2c101e8bb3f08ef9120d82c2b20600b` before the W00
  commit.
- `make test`: passed (`1 passed` against the imported scaffold).
- `make docs-check`: passed.
- `make skill-check`: passed.
- `make secret-scan`: passed using the repository's then-current lightweight
  fallback because local `gitleaks` was not installed. W01 subsequently made tree/history scans
  fail closed and self-testing; G17 still requires independent release scans.
- W00 PR #1 initially exposed one gitleaks false positive in threat-model test
  prose. The prose was rewritten and only the exact historical finding
  fingerprint was suppressed in `.gitleaksignore`; no rule/path/general value
  allowlist was added.
- At prior evidence head `11652eff2ec46f9c895c41a1c1a16aa28eac70b8`,
  GitHub reported PR #1 mergeable and both protected `test` and `gitleaks`
  checks successful. Live branch protection retained strict checks, admin
  enforcement, required conversation resolution, and disabled force pushes
  and deletion.
- Targeted current-tree and full-public-history scans found no private personal
  label/path or credential-shaped value. The only `Tokru` match is the explicit
  prohibited-donor example in the authoritative plan.
- All checked-in TOML/JSON and GitHub YAML parsed successfully.
- `git diff --check`, ADR file/link/plan-anchor validation, and the
  duplicate-workflow absence checks passed.
- No private donor command wrote to the donor tree, and no private history,
  configuration, fixture, telemetry, report, or source file was imported.
- A01 local validation passed: `make docs-check`, `make skill-check`, `make test`
  (`2 passed` on Python 3.12.8/macOS), `make secret-scan`, `git diff --check`,
  JSON parsing, local Markdown-link resolution, and all five upstream
  `quick_validate.py` skill checks. Local secret scanning used the documented
  lightweight fallback because gitleaks is not installed; hosted gitleaks remains required.
- The deterministic negative fixture correctly rejected an injected `TODO` placeholder. The
  disposable fixture was outside the repository and was deleted after the test; no raw agent
  content was retained.
- [skill-evaluations.md](skill-evaluations.md) records sanitized behavior and adjacent-negative
  evidence for all five skills without raw prompts, responses, transcripts, or tool output.
- The first A01 gate review returned two P2 findings: static CI had been conflated with behavioral
  evidence, and W00 was incorrectly labelled externally pending. Both were corrected. Read-only
  re-review found no new P0-P2 and returned `externally_pending` only for commit and hosted checks.
- Supplemental pre-merge audits found one P2 in publication-checklist state and four P2s in skill
  metadata, canonical-source, context-parity, and privacy validation. The checklist now separates
  the completed administrator draft test from the open G17 third-party smoke. Validation now parses
  the exact metadata schema, rejects canonical-source symlinks, enforces hierarchy/role/mutation
  parity, scans every skill/evidence context, and includes negative regressions for each defect.
- A later validation audit found two additional P2s: SKILL frontmatter could pass malformed YAML,
  and private Linux user or privileged-home paths were not detected. All five frontmatters now use
  an exact quoted-string schema with duplicate/type/mapping regressions; Linux skill/evidence path
  regressions complete the synthetic privacy matrix. The final local suite passes 14 tests.
- Final report-only review of head `46f99e0a3af6741cfb2089b4672116ec0e04defc` found no remaining
  P0-P2. Protected `test` and `gitleaks` passed on that exact head before merge.
- PR #1 merged at 15:23:58Z as signed squash commit
  `79b9fdca3c567b1de48a3136fbe4ba0dd981926a`. Its tree exactly matched the reviewed head; protected
  post-merge [`test`](https://github.com/that1guy15/Milhouse-oss/actions/runs/29692758580) and
  [`gitleaks`](https://github.com/that1guy15/Milhouse-oss/actions/runs/29692758558) both passed.
- E01 private-path evidence: enabled read-back succeeded; the self-report endpoint returned GitHub's
  expected sole-administrator HTTP 403; an administrator-side `[TEST ONLY]` low-severity draft was
  created at 14:41:07Z, read back privately, and closed at 14:41:22Z. `published_at` remained null,
  no CVE or private fork was created, and the remaining triage count was zero.

W00, E01 for G00, E02, and A01 are complete. The independent third-party PVR delivery smoke remains
deferred to E01/E05 at G17. W01 began from the exact protected-main G00 commit.

## External action register

These actions cannot be inferred from “build it.” Engineering must prepare the
exact request/evidence, and the named owner must authorize or perform it.

| ID | Earliest gate | Accountable actor | Required action and evidence | Current state |
|---|---|---|---|---|
| E01 | G00/G17 | Repository owner | Enable GitHub private vulnerability reporting and test private advisory visibility and closeout without publishing sensitive content | Complete for G00 on 2026-07-19: enabled read-back passed; sole-admin self-report produced GitHub's documented 403; admin-side synthetic draft create/read/close passed with no publication, CVE, fork, or remaining triage report. Re-run a true third-party reporter-to-reviewer smoke with E05 at G17 |
| E02 | G00/G01/G17 | Repository owner | Maintain a usable solo-maintainer PR path; retain strict protected checks/admin enforcement/no force-push/deletion; move to required human/CODEOWNER review when a second maintainer exists | Complete for G00/G01 on 2026-07-19: protected PRs #1, #2, and #5 merged through the authorized path; the sole strict aggregate `required-ci` context, admin enforcement, conversation resolution, and no force-push/deletion controls remain active. Revisit human/CODEOWNER review at G17 or when a second maintainer exists |
| E03 | Before first public package/tag | Project owner | Verify project/distribution-name availability and complete any desired trademark/legal review; approve a plan amendment if names change | Pending |
| E04 | G12–G14 | Operator/project owner | Supply sandbox-only credentials and explicitly authorize each live provider/session-format/notification/action call; approve the redacted destination and retain the smoke record | Pending; fixture work may proceed |
| E05 | G17/G18 | Project owner and independent reviewer | Select an independent reviewer for security, provenance, package contents, and release evidence; record reviewer identity, scope, findings, fixes, and acceptance | Pending |
| E06 | G06/G15–G18 | Project owner/host owner | Supply or authorize required clean macOS 14/latest and Ubuntu 22.04/24.04 hosts when engineering cannot; retain exact environment and artifact-hash evidence | Pending until needed |
| E07 | G18 | Project owner/elapsed monitor | Keep the approved environment available for the 7-day alpha, 14-day beta, and 7-day RC soaks; preserve continuity evidence and respond to monitor alerts | Pending |
| E08 | G17/G18/release | Project/release owner | Build-branch push/PR/merge is authorized. Separately authorize protected release environment, signed tag/build, Trusted Publishing, GitHub Release, any visibility change, and announcement. Approval for one step does not imply the next | Branch push/PR/merge authorized 2026-07-19; release actions pending |
| E09 | Release completion | Project owner/release monitor | Authorize public-registry installs, announcement only after verification, and at least 72 hours of post-publication monitoring | Pending |
| E10 | Active implementation | Project owner and maintainers | Publish human-readable engineering-journal posts from meaningful `merged_verified` milestones using only public evidence, explicit implemented/planned boundaries, and the checked-in privacy template; prepare a paired unpublished personal-learning draft or scoped series plan for nonexpert readers; save every completed Discussion-derived learning blog as an unpublished Substack draft through the owner's authenticated Chrome session | GitHub Discussions `Announcements` authorized 2026-07-22. On 2026-07-29 the owner directed that each Discussion workflow produce an unpublished beginner-friendly learning companion and separately authorized the authenticated Substack create/save handoff for every completed blog derived from current-work or feature Discussions. Verify **Saved** and stop: publication, scheduling, email delivery, account changes, release or availability claims, package publication, and unrelated external mutation remain unauthorized |

## Defects, amendments, and stop conditions

| ID | Date | Decision | Scope and disposition |
|---|---|---|---|
| A01 | 2026-07-19 | Owner-approved Compound Engineering process adaptation | Ratified by ADR 0015. Uses `EveryInc/compound-engineering-plugin@8163a96e86656a89797869ac61905fe4641f81be` as a concept-only MIT reference. Establishes five Milhouse-native skills, discovery aliases, read-only gate review, sanitized learning, and explicit privacy, egress, and mutation boundaries. No public API, stored schema, privacy promise, product scope, or release gate changed |
| D01 | 2026-07-22 | **P1** PR #21 squash-message DCO incident; accountable owner: engineering delivery | Affects W02/G02 acceptance and G17/G18 DCO and release provenance. Protected squash `76511d5c63e4509765b3ad3ceabefba251e559c7` has literal escaped newlines and no parseable author-matching trailer, while its signed source `74ca504c9483e6af983e829e2e831af6b00d9061` has the identical reviewed tree. Post-merge run [`29902382356`](https://github.com/that1guy15/Milhouse-oss/actions/runs/29902382356) reproduces the correct DCO and aggregate failure; all other jobs passed. [D01 evidence and bounded recovery](gate-evidence/PR21-DCO.md) record signed protected remediation `c0f9f2a8a1300eef18e651a20b6e2111d9cbd6a5`, exact-tree review, green PR/post-merge checks, and continued rejection of the affected range. Immediate recovery is complete. The original squash remains noncompliant; its permanent exact historical disposition is recorded by amendment A03 (2026-07-22, ADR 0017) and must not weaken future enforcement |
| A02 | 2026-07-22 | Owner-approved persisted local structured-log contract | Ratified by ADR 0016 and plan section 4.15, adding the `local_log` egress surface to section 4.7. Defines the bounded `CanonicalJSONV1` JSONL wire, secured namespace, durability, rotation, crash recovery, lock-ordered concurrency, bounds, and 14-day retention/lifecycle, split across W02, W03, W06, and W16. Installation-scoped local operational metadata only; no external egress, record, publication, or retention expansion beyond the existing 14-day log class |
| A03 | 2026-07-22 | Owner-approved exact D01 historical DCO disposition | Ratified by ADR 0017. Binds only PR #21 squash `76511d5c63e4509765b3ad3ceabefba251e559c7`, parent `15eb96dc23fb25b7787f1f6d3c7563a9ccd525cf`, tree `9629b5116e6351cee94c5385a9a5a20ed93abf69`, signed source `74ca504c9483e6af983e829e2e831af6b00d9061`, and protected recovery `c0f9f2a8a1300eef18e651a20b6e2111d9cbd6a5`. The original squash remains permanently noncompliant; the matching author's signed follow-up supplies the narrow retroactive attestation. No generic exception, checker weakening, history rewrite, or force push; future DCO enforcement unchanged |
| D02 | 2026-07-23 | Gate-scope inconsistency: G02 required dependency-blocked downstream surfaces (owner-approved correction, plan amendment A04) | The plan W02 G02 assertions and ADR 0016 revised-validation required concrete report (W09), structured-log file (W03), CLI/stderr/diagnostics (W06), and backup/restore/purge (W16) surfaces owned by dependency-blocked later packages, contradicting the section 4.15 ownership split and creating a G02/W03 gate cycle; the G02 evidence packet also bound its coverage result and acceptance to a pre-correction commit and lacked full per-slice traceability. Resolved by owner-approved plan amendment A04 (2026-07-23): the G02 assertions are re-scoped to certify only the W02 redaction/rendering/structured-error/structured-event and `local_log` wire and stream-sink primitives, deferring the concrete report surface to G09, the file surface to G03, and the CLI/stderr/diagnostics surfaces to G06; the plan, ADR 0016, and [G02.md](gate-evidence/G02.md) are aligned and re-anchored to `fad1479` with a completed per-slice provenance table added. This packet supersedes the withdrawn PR #32; acceptance binds to this packet's protected merge commit. No source code changed; surfaced by the PR #31 and #32 owner reviews |
| A04 | 2026-07-23 | Owner-approved W02 G02 gate-scope alignment (resolves D02) | Re-scopes the plan W02 G02 assertions and ADR 0016 revised-validation to certify only the W02 redaction, rendering, structured-error, structured-event, and `local_log` wire and stream-sink primitives; defers the concrete generated-report surface to G09, the structured-log file surface to G03, and the CLI/stderr/diagnostics surfaces to G06 per section 4.15. Preserves the privacy invariant unchanged; changes no product scope, retention rule, or later-gate content; removes the G02/W03 gate cycle |
| A05 | 2026-07-23 | Owner-approved exact v1 stored-log schema | Ratified by an ADR 0016 addendum and plan section 4.15; merged in PR #33 (`fad1479`). Promotes the stored-log wire from a prose field summary to an exact, machine-checked v1 schema: literal keys and canonical order, `line`/`schema` values, scalar types, explicit-`null` optionality, RFC3339 UTC millisecond timestamps, 64-character lowercase-hex digest, per-line and per-segment byte bounds, empty-segment semantics, digest coverage, and `expires_at = opened_at + retention_days` (tighten-only; W02 encodes, W03 computes), binding the header `retention_days` domain to the `[retention].logs_days` ceiling. Changes no already-implemented wire byte; locked by `tests/unit/test_log_schema_contract.py` |
| D03 | 2026-07-28 | **P1** filesystem-containment objective omitted the installation-account trust boundary; accountable owner: engineering delivery | Affects W03/G03 and later security/release review. Threat-model objective 4 was an unconditional write-containment claim, while plan section 4.8 and ADR 0008 already treat explicitly installed in-process plugins as trusted code with the Milhouse user's authority. On supported POSIX hosts, ancestor-pathname validation cannot be atomically bound to a later descriptor-relative mutation against hostile same-UID interference. The affected W03 workstream stopped for owner decision. Resolved at the contract level by owner-approved A06. PR #44's separate code findings were corrected and its exact final head passed independent review before merge. PR #45's merged-tree findings were tracked separately by D04 and are now corrected and merged (PR #54, `1855d36`) after an independent exact-head PASS review |
| D04 | 2026-07-29 | **P1** PR #45 final head merged without exact-head PASS; accountable owner: engineering delivery | Protected `main` `454736f46c0bff73afd248273774354e1294e11b` has the same tree as PR #45 source head `9b51e6f6647f86aa8c061457db3758df1d79c49d`. The final source and protected squash both have green Required CI, but the exact final head was not independently reviewed before merge. The post-merge audit recorded on PR #53 reproduces five P1 defects: nested maintenance can deadlock while reacquiring its held barrier; plan-valid nested `[paths].logs` is rejected; the rotation bound can retain an expired active segment; a symlinked ancestor can redirect the log store; and recovery deletes an unauthenticated staging-name occupant. **Resolved 2026-07-29 by PR #54, protected `main` squash `1855d36cfe7886569af83509af1f3abb20f6fffc`:** all five defects corrected with adversarial regression evidence (`tests/unit/test_log_d04.py`), an [independent report-only exact-head review](https://github.com/that1guy15/Milhouse-oss/pull/54#issuecomment-5124132499) returned verdict PASS (its one residual P2 — an over-permissive torn-header prefix — tightened in-branch), and green pre-merge [30475228446](https://github.com/that1guy15/Milhouse-oss/actions/runs/30475228446) and post-merge [30475467655](https://github.com/that1guy15/Milhouse-oss/actions/runs/30475467655) Required CI. G03 remains **In progress** for the remaining slice-7 evidence, no longer blocked on these findings |
| A06 | 2026-07-28 | Owner-approved installation-account filesystem-containment boundary | Ratified by the plan section 1 register, threat-model objective 4 and residual-risk update, and an ADR 0004 addendum. Trusts the operating-system account running Milhouse and the host administrator for filesystem containment; retains enforced protection against untrusted input, traversal, symlinks, other local users, and cooperating Milhouse processes; documents the adjacent-syscall hostile same-UID residual; requires uncertain rather than successful outcomes after detected namespace drift; retains adversarial tests; and claims no hostile same-UID containment. This narrows the literal security attacker model but changes no wire, stored schema, retention, egress, product scope, migration, tag, publication, or release authority |
| A07 | 2026-08-01 | Owner-approved installation-key provenance + reserved compaction-namespace upgrade contract | Ratified by an ADR 0007 addendum, the plan §1 amendment register (full change-control record: reason, alternatives, compatibility, migration, security, revised tests), and a plan §4.7 edit; registered in the ADR index. Resolves the deep P1s independent reviews reproduced in the W03 audited-compaction remediation (PR #79, defects D07/D08). **First-draft A07 was insufficient and is corrected here per the owner's 2026-08-01 decisions** (wire full key-ID/epoch binding now; auto-converging legacy-occupant migration). **(1) Installation-key provenance bound to the control-plane identity:** a keyed audit derivative is trustworthy only if produced by the installation's own key — `Pseudonymizer` is constructible from any 32 caller-supplied bytes, and a wrong/restored/cross-installation valid key can sit at the bound path, so neither the exact-type gate nor merely loading the file proves identity. W03 records the installation's non-secret pseudonym key ID and epoch in the SQLite control plane (**migration 11, the singleton `_installation_key` table**); `init` (W06) writes that record when it creates the key file; and compaction **binds its config/runtime `state_root` to the control database's state root, reads the recorded key ID/epoch, loads with `load_pseudonym_key(config, paths, epoch=<recorded>, expected_key_id=<recorded>)`, and fails closed before any file/ledger/cursor/audit mutation** on an absent record (unprovisioned), a missing/unloadable/malformed key, or an ID/epoch mismatch. Compaction accepts NO caller-supplied key and its audit constructor is non-optional; until `init` establishes the record it fails closed and cannot run — a fail-closed contract recorded honestly, not a passing path. **(2) Reserved namespace, unexported authority, and auto-converging upgrade:** the `c[0-9a-f]{64}` successor namespace is reserved from schema 10; the commit ingress rejects it and only compaction publishes into it through an **unexported reserved-only authority** — the public `publish_segment_bytes`/`write_spool_segment` surface rejects reserved names with NO caller bypass. Because `c`+64-hex is a legal producer batch id, a pre-reservation schema could hold a committed reserved-namespace segment, so on acquisition a **restartable exclusive-barrier remediation auto-converges** it by rewriting to a fresh non-reserved id (all records preserved, cursors re-pointed, old file retired under a durable tombstone) — a legal install is **never wedged** and expired data is never retained. Compaction also records a **durable retirement tombstone for the superseded old segment in the same transaction as the ledger swap**, and reconciliation completes that deletion only behind a positive day-directory durability fence, never re-adopting the retired bytes. In pre-alpha (no released install) the legacy-occupant state is empty and the remediation is a deterministic, crash-safe no-op. Changes no wire byte, retention rule, product scope, tag, publication, or release authority beyond the schema-11 addition; preserves every privacy invariant |
| D05 | 2026-07-30 | **P1** slice-4/5 merged trees carry four unresolved P1 contracts, and this ledger recorded overstated review evidence; accountable owner: engineering delivery | Affects W03/G03. An independent gate review of the exact merged heads of PRs #56/#58/#60/#62 (and the docs PRs #59/#61/#63) failed all six and reproduced four P1s not caught before merge: (1) **delivery integrity** — `deliver_segment` records a terminal `delivered` for a segment while forwarding caller-supplied frames that need not belong to it, so a destination can receive the wrong records while the ledger and `verify_spool` certify success (it does not bind the barrier to the database, validate the exporter object, reload the live row, validate the frames against the record, or require the final compare-and-set to affect the expected row); (2) **audit privacy** — the `_audit` API's validators enforce only bounded non-empty strings, so a maintenance caller can persist raw secrets/emails/paths/identifiers unchanged into control state, contrary to plan §§4.3–4.4 and ADR 0007; (3) **privacy-expiry hard bound** — the non-cascading `_cursors.batch_id` foreign key blocks `retention_apply`'s `_segments` delete for a cursor-referenced segment, and retention converts the resulting integrity error into a silent skip, so a privacy-expired segment held by an idle source's cursor is never pruned; (4) **false review/completion evidence** — this ledger asserted "independent exact-head PASS/no-P0-P1" reviews and "slice 5b complete", but those were same-session in-process adversarial subagent checks, not independent public GitHub review artifacts (GitHub history holds zero reviews/comments on #58–#63), and they missed the P1s above, so the durable ledger overstated the evidence. Also open: P2 shadow-spool verification (`verify_spool`/replay accept any root), P2 post-unlink orphan misclassification, and P2 the secure-unlink primitive not enforcing exact `0600`/ACL. **Disposition:** the PASS/complete language is withdrawn (below); slice 5b is **not** complete; G03 stays **In progress**. The overstated-evidence finding (4) was corrected first by PR #64 (`5bf2a95`). **All four P1s and three P2s are remediated on branch `codex/w03-5b-remediation` (this PR)** with adversarial regression evidence: migration 7 recreates `_cursors` with a nullable `batch_id` and retention detaches the cursor (preserving position/revision) before the delete so a privacy-expired cursor-referenced segment now prunes; the free-text audit sink is replaced by the typed `record_retention_prune` constructor that admits only an opaque-id resource; `deliver_segment` binds the barrier, reloads and matches the live row, requires the frames to hash to `content_sha256`, checks exporter self-identity, and treats the CAS as authoritative; `verify_spool`/`replay_segments` bind the spool root to `<state_root>/spool`; the secure removal primitive validates the leaf's exact `0600`/ACL from its descriptor and distinguishes a post-unlink commit-uncertain outcome; and retention reports a tri-state `file_outcome`. That remediation merged to `main` as PR #65 (`da9b9cd`). **A fresh independent gate review on 2026-07-30 of PR #65's exact head FAILED it with four P1s and one P2, disproving the in-process "no-P0/P1/P2" re-review that was explicitly NOT the acceptance gate:** (a, **P1**) `deliver_segment` validates the caller-owned frame `Sequence` but then forwards that same unsnapshotted object, so a sequence that yields different contents on the second read can deliver batch B while certifying batch A — the snapshot must be frozen to an immutable tuple once and both validated and forwarded; (b, **P1**) delivery holds no shared authority across reload, forward, and checkpoint, so privacy-expiry `retention_apply` can prune a segment mid-delivery, the expired batch is still forwarded, and the zero-row CAS is reported as `already_delivered` — conflating a missing row with a delivered one, contrary to the plan §§4.3/4.8–4.9 global-writer and hard-expiry contracts; (c, **P1**) the audit resource validator is only a character-class check, so an access-key-shaped canary (`AKIAIOSFODNN7EXAMPLE`, a legal batch-id shape) was persisted unchanged into `_audit` — the resource must be stored as a safe fingerprint, not raw caller text; (d, **P2**) a post-unlink descriptor-close failure is still mapped to `UNREADABLE` and reported as an orphan though the file is gone; and (e, **P1**) this ledger's "all four P1s and three P2s are remediated" assertion was itself disproven. **Slice 5b remains not complete; G03 stays In progress; no W03 advancement or slice-5c merge until these are remediated with adversarial regression evidence AND a fresh independent full-SHA review passes.** **Re-remediation (PR #67, branch `codex/w03-5b-remediation2` → `main` `364d27c`) fixes all five: (a) `deliver_segment` freezes the caller frames to one immutable tuple and validates+forwards that exact tuple; (b) the whole reload→forward→checkpoint runs under one `barrier.shared()` hold that fences the exclusive-side retention, plus a `now`-based hard-expiry gate withholds any segment with an expired record; (c) the delivery compare-and-set distinguishes `segment_gone` from `already_delivered`; (d) `record_retention_prune` stores only a SHA-256 fingerprint of the resource, so `_audit` can never hold raw caller text even for a charset-legal secret-shaped id (credential canaries added); (e) a post-unlink descriptor-close failure maps to `COMMIT_UNCERTAIN`. Each has adversarial regression evidence; full local + CI gates green. Merged 2026-07-30 on the owner's DIRECT APPROVAL (owner acceptance under external mutation authority) — this was NOT an independent public full-SHA review, which remains advisable before release-gate review (E05).** The slice-5b remediation is accepted; G03 stays In progress for slice 5c and slice 7. No prior evidence is rewritten; the corrected reproductions supersede the withdrawn "remediated" claim |
| D06 | 2026-07-31 | **P1** slice-5c compaction/audit code carried three latent P1s and one P2 caught by the slice-7 G03 review; accountable owner: engineering delivery | Affects W03/G03. An independent gate review of the slice-7 candidate (PR #72) re-examined the compaction/audit code (merged earlier via PR #69) that the crash/failure-injection harness exercises and reproduced: (1) **P1 privacy stranding** — the slice-5c collision fix REFUSED compaction when the deterministic successor id was occupied by a foreign segment, so a mixed-expiry segment's expired frames were retained indefinitely past their hard privacy deadline (the earlier fix had traded data-loss for stranding); (2) **P1 no attributable audit lineage** — production compaction supplies no pseudonymizer, so both `record_compaction` rows persist `resource` NULL, and plan §4.7 requires a keyed installation pseudonym whose key is created by `init` (W06), so the keyed lineage is genuinely deferred to W06; (3) **P1 unvalidated pseudonym output** — `_keyed_resource` persisted `pseudonymizer.fingerprint()` output without validating it, so a hostile pseudonymizer could write a secret-shaped value into `_audit`; (4) **P2 false provenance** — a compacted segment recorded a reconstructed day-start `committed_at` with `origin=committed`, though the ledger reserves reconstructed-midnight instants for `origin=reconciled`. **Remediated on branch `codex/w03-slice7` head `9128fad` → `main` `ced5513`:** compaction probes a bounded deterministic alternative id sequence (reuse-own-successor-or-first-free), so a collision can never strand expired frames; `_keyed_resource` validates the exact canonical `mh_fp1_e{epoch}_batch_{base32}` grammar and contains a raising/non-string pseudonymizer, failing closed with a fixed code and persisting nothing; the compacted segment uses `origin=reconciled`; and the G03 evidence packet records the compaction-lineage keyed identifiers as **pending/deferred to W06** (not passed), with a keyed-path regression proving attributable lineage when a key is supplied. Each has adversarial regression evidence; full local + CI gates green (test-coverage line 97.97% / branch 96.95%, compaction.py 100% branch). Merged 2026-07-31 on the owner's direct approval (owner acceptance, NOT an independent public full-SHA review). **This "remediated / all seven slices complete / G03 candidate" language is now WITHDRAWN.** A fresh independent gate review on 2026-07-31 of the integrated head (`main` `b2822f3`, tree `6d89f6b`; equivalently PR #72 head `9128fad`, whose tree is identical to the merged `ced5513`, so no later production commit addressed it) returned verdict **FAIL** with four P1 blockers and one P2, disproving the remediation: (a, **P1**) the bounded collision *probe* still strands — `_resolve_successor` searches only a finite, publicly predictable id set and then reports `collision_bound`, leaving the mixed-expiry source segment intact, so occupying every candidate retains expired data past its hard privacy deadline (probing did not eliminate the stranding, only the trivial single-collision case); (b, **P1**) grammar validation is not proof of keyed derivation — `_keyed_resource` validates only the *shape* of an overridable `fingerprint()` result, so a `Pseudonymizer` subclass/proxy can persist caller-controlled canonical-shaped input into `_audit`; derivation must occur inside a trusted, non-overridable boundary (the exact concrete `Pseudonymizer` type); (c, **P1**) the compaction lineage cannot be deferred to W06 by an evidence-document note — production compaction still records NULL lineage, plan §4.7 makes keyed old/new lineage a locked W03 deliverable, and W06 is downstream of G05 while W05 depends on G03 (a dependency cycle), so discharging it requires either wiring installation-keyed lineage into production W03 compaction OR an explicit owner-approved plan/ADR amendment — an owner decision, pending; (d, **P1**) this ledger's own "remediated / all seven W03 slices complete / G03 candidate" assertion was itself disproven by (a)–(c); and (e, **P2**) the G03 packet is not bound to final exact-head evidence (recorded `4644 passed`, no tested SHA/tree/coverage). **Disposition: slice 5c and slice 7 are NOT complete; W03 and G03 return to In progress; no G03 acceptance and no further W03 advancement until (a)–(c) are remediated on a new protected-`main` SHA with adversarial regression evidence AND a fresh independent full-SHA review passes, plus the pending owner decision on the compaction-lineage ownership, supported-host physical-crash evidence (E06), and the packet refresh (e).** No prior evidence is rewritten; the corrected review supersedes the withdrawn "remediated" claim. **Remediation candidate (2026-08-01, NOT yet accepted): the four P1s and the P2 are addressed across two PRs — the ledger integrity correction (d) landed as PR #75 → protected `main` `3c2e14f`, and the code fixes (a)/(b)/(c) + the packet refresh (e) are on branch `codex/w03-g03-remediation` (PR #76, this branch).** (a) The `c[0-9a-f]{64}` compaction successor namespace is now RESERVED at the producer commit ingress (`commit_segment` → `MH_SPOOL_RESERVED_ID`); compaction inserts its own rows directly, bypassing that ingress, so a foreign occupant of the derived id is reachable only by a SHA-256 collision. The bounded probe is REMOVED — compaction derives a single deterministic successor (free, or its own successor for crash recovery); a non-successor occupant fails closed loudly (`reserved_conflict`), never silently stranding and never retiring the old segment against foreign data. (b) `_keyed_resource` now requires the exact concrete `Pseudonymizer` type (`type(pz) is Pseudonymizer`, runtime import) before deriving, so a subclass/proxy cannot persist caller-controlled canonical-shaped input; grammar validation is retained as defence in depth. (c) Per the owner's 2026-08-01 decision to **wire keyed lineage into W03 now** (not amend/defer), the installation `Pseudonymizer` is threaded through `compact_apply → _compact_one → _swap_ledger → record_compaction`; a maintenance caller loads it via the existing W02 `load_pseudonym_key` (compaction has no production caller yet), so keyed old→new lineage is recorded whenever a key is present and is `NULL` only pre-provisioning — discharging plan §4.7 as a W03 deliverable. Adversarial regressions added: producer-reserved-namespace rejection; single-derived-id successor in the reserved namespace; fail-closed on a foreign row / unreadable file / foreign file at the reserved id; a `Pseudonymizer` subclass with canonical output rejected; end-to-end keyed compaction lineage via a real `Pseudonymizer`. Full local gates green on candidate code head `e452756` (tree `53761246`; CPython 3.14.6 / macOS 26.5.2): test-coverage **4661 passed / 1 skipped**, line **98%** (`line=97.96% branch=96.91%`), critical files (`compaction.py`, `commit.py`, `segment.py`, `audit.py`) above the 95% branch floor. **Remediation MERGED → protected `main` `91b97b03d8b7b503a895697ac10a0931b9a229c0` (PR #76 squash, 2026-08-01):** the squash tree `fb372e297fea131da767896cdad2dadaa44f51ed` equals the reviewed PR-head tree, its `src/` is byte-identical to the gate-tested `e452756` (the second PR commit changed only docs), and post-merge Required CI [run 30702416917](https://github.com/that1guy15/Milhouse-oss/actions/runs/30702416917) passed. **This is recorded as remediated-PENDING-independent-review, NOT accepted: G03 is now READY FOR a fresh independent full-SHA review of `91b97b0`, but stays In progress until that review PASSES, plus E06 supported-host physical-crash evidence and owner acceptance. This ledger records the merge and CI; it does NOT assert an independent review occurred** |
| D07 | 2026-08-01 | **P1s across two independent reviews plus a PR #77 packet overstatement** — the 2026-08-01 G03 remediation was insufficient; accountable owner: engineering delivery. **This entry records the live review artifacts SEPARATELY and exactly (an earlier draft wrongly combined them into one "five-P1 review" — itself an evidence-integrity defect this corrects).** | Affects W03/G03. Two distinct independent gate reviews and one ledger/packet overstatement, recorded per their live GitHub artifacts (SHAs/URLs/timestamps/severities/test counts re-fetched from the source comments): **(A) PR #74 review** — [comment 5148867847](https://github.com/that1guy15/Milhouse-oss/pull/74#issuecomment-5148867847), 2026-08-01T01:32:04Z, exact head `a2e71b7d15e03eb22d4b44b0cf7c2e99b76ea457`, base `b2822f3`, verdict **FAIL**, **8 focused exact-tree tests passed plus a separate 1 probe-exhaustion adversarial reproduction passed** (both recorded by comment 5148867847), with a deterministic exact-tree reproduction of the crash re-adoption (reviewing the physical-crash-procedure doc + integrated state). Its findings that apply to the merged code: **[P1] retention crash re-adopts privacy-expired bytes** — `spooling/retention.py` `_prune_segment` commits the ledger-row delete before unlinking the file; a crash leaves the expired file, startup reconciliation re-registers it as a committed segment with NO durable prune intent, and restart/reconcile alone never finishes the deletion (a separate later `retention_apply` is required), violating the plan's hard privacy upper bound; **[P1] the bounded successor probe still strands** mixed-expiry data (1,024 caller-predictable candidates → `collision_bound` with the old segment intact); **[P1] audited compaction has no attributable production lineage** (production `_swap_ledger` calls `record_compaction` with no pseudonymizer → NULL resources); **[P2] the physical-kill procedure lacks reproducible internal kill controls / a fixed host matrix.** The retention source blob is unchanged through the merged candidate, so its P1 applies. **(B) PR #76 review** — [comment 5151585903](https://github.com/that1guy15/Milhouse-oss/pull/76#issuecomment-5151585903), 2026-08-01T13:16:07Z, exact head `f9ce1894e5fced16e5e9b67a0260e8a1fd43f8fe`, base `3c2e14f`, tree `fb372e297fea131da767896cdad2dadaa44f51ed` (= the PR #76 squash `91b97b0`), verdict **FAIL with three P1 + one P2**, 149 targeted tests, predating the merge: **[P1] a valid same-survivor policy collision still strands** — `_derive_batch_id` hashes only surviving `record_id`s while `_is_intended_successor` binds exporter/policy/content, so two mixed segments with the same survivor id and different exporters derive the same reserved id, the second returns `reserved_conflict`, and its expired frame is stranded (reproduced; no SHA-256 collision); **[P1] keyed production lineage optional and unwired** — `compact_apply` defaults `pseudonymizer=None` with no production caller → NULL `_audit` resources; **[P1] the audit omits the mandated old/new hashes** — `record_compaction`/the `_audit` schema carry no old/new content/file digests, so lineage is not cryptographically bound to the retired/produced bytes; **[P2] the refreshed packet is not bound to the exact head.** **(C) PR #77 packet overstatement** — PR #77 (merged to `main` `c7390ed`) has ZERO posted reviews/comments; its `G03.md`/status update asserting "each finding addressed / candidate ready / head under review 91b97b0" is a LATER ledger/evidence defect (an overstatement made after PR #76 merged), NOT a posted review of PR #76. **Corrections (from the reviews):** persist a durable privacy-safe retirement/tombstone intent before the row delete and have reconciliation finish the unlink within a bound (retention); derive/durably-reserve the successor from the complete intended identity + a source discriminator and enforce reserved provenance at every publish/reconcile ingress, so a terminal conflict never strands (collision); make the installation `Pseudonymizer` MANDATORY at the mutating boundary or wire a real maintenance entry point that loads/verifies the key (lineage); add a migration + privacy-safe old/new content+file digests written in the swap transaction, rolling back on mismatch (hashes); and record every review artifact truthfully (evidence integrity). **Disposition: the "remediation-ready / findings addressed" language of PR #76/#77 is WITHDRAWN; G03 is NOT ready and NOT accepted; W03 and G03 stay In progress. The defects must be remediated on a NEW production-`main` SHA with adversarial regression evidence AND a fresh independent full-SHA review before any G03 acceptance.** **(D) PR #79 review (the first code-remediation attempt)** — branch `codex/w03-g03-rem2`, exact head `1b19a91efad6f145b1d971a06a43053be87000bb`, [comment 5152583787](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5152583787), 2026-08-01T17:36:05Z, verdict **FAIL with four P1s**, 178 targeted tests + three adversarial reproductions (hosted full suite 4671 passed / 2 skipped): **[P1] a commit-uncertain unlink can lose its tombstone before the deletion is durable** — reconciliation clears an absent-file tombstone from bare namespace observation with no durability fence, so a power-loss that rolls back the un-fsync'd unlink resurrects the expired file with no intent; **[P1] the "trusted installation" lineage key is still caller-forgeable** — `type(pz) is Pseudonymizer` passes for `Pseudonymizer(b"Z"*32)` (any 32 bytes), and nothing binds the key's `key_id`/epoch to the installation; **[P1] the reserved namespace does not handle legal pre-upgrade `c[0-9a-f]{64}` occupants** — those ids were producer-writable before the reservation and migrations 9/10 do not inventory/transform them, so an upgraded install can leave a mixed segment permanently uncompactable; **[P1] the "all five P1s remediated" evidence claim** omitted the fifth (evidence) fix, cited a #78 head that itself FAILED, and misstated the count (local 4672/1 vs hosted 4671/2). **Owner-approved amendment A07 (2026-08-01, this row's sibling) scopes the two deep findings' contract:** compaction establishes key provenance by LOADING inside the config/paths boundary and fails closed on an unprovisioned/wrong/absent key (no caller-supplied key); the reserved namespace is reserved-from-schema-10 with a restartable exclusive-barrier fail-closed upgrade check. **Disposition: the code remediation is being reworked per A07 on a NEW head — NOT yet reviewed or merged; every remediation SHA lands only behind its own fresh independent full-SHA review with no P0/P1.** Neither the PR #74/#76 review reran the canonical full suite (host `uv 0.7.10` vs the repo-locked `0.11.29`), so their author-hosted 4661/coverage/10k figures remain author evidence |
| D08 | 2026-08-02 | **Round-2 remediation FAILED both PRs — the docs correction and the code rework were each independently reviewed and each returned FAIL; accountable owner: engineering delivery** | Affects W03/G03. Two independent gate reviews, recorded per their live GitHub artifacts (SHAs/URLs/timestamps/severities/test counts re-fetched from the source comments). **(A) PR #78 review** (the docs/A07/ledger correction) — [comment 5154004969](https://github.com/that1guy15/Milhouse-oss/pull/78#issuecomment-5154004969), 2026-08-01T23:34:59Z, exact head `349150e2c7434e200f1f39c24cbbe4c04ee8a779`, base `c7390ed023789f9839d6a5a2858fc73b223cfca5`, verdict **FAIL with three P1 + one P2**, 54 focused plan/gate/tool/scaffold tests passed (docs-check 70 files / 254 links / 78 external probes; `src/`/`tests/` unchanged): **[P1] A07 loaded a key but did not bind it to the installation identity** — `load_pseudonym_key` defaults `expected_key_id=None`, so a different installation's valid 32-byte key at the exact secure path passes, and the amendment's "fails closed on a wrong key" promise was false; loading proves file provenance, not installation identity; **[P1] the gate packet and PR still contradicted the live review record** — `G03.md` said "no head currently under review" and PR #79 "NOT yet independently reviewed" while also recording #79 head `1b19a91`'s four-P1 FAIL, and twice called comment 5148867847 a second review of the exact PR #76 tree though it reviewed the distinct PR #74 head `a2e71b7d`; the PR description still claimed a "five P1 blockers" exact-head review; **[P1] A07 did not satisfy the plan's amendment change-control contract** (§1 requires reason, alternatives, compatibility/migration/security impact, and revised tests, plus ADR-index registration), and the status row is evidence authority, not the plan amendment; **[P2] D07 omitted PR #74's separate `1 probe-exhaustion` test result** while claiming exact test-count reconciliation. **(B) PR #79 review** (the code rework) — [comment 5154152313](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5154152313), 2026-08-02T00:18:32Z, exact head `9f0947aa848a9f19d022a2dec4fa02c7a469cc5a`, base `c7390ed0`, tree `e75ea081454c2cab00b5d700c592694b9558c0dc`, verdict **FAIL with four P1**, exact-tree changed-area suite 180 passed + reconciliation/schema/key/writer suite 241 passed + three adversarial reproductions: **[P1] the public reserved-namespace escape hatch recreates collision stranding** — `publish_segment_bytes(..., allow_reserved=True)` stays publicly exported, so a direct publisher writes a foreign segment at the reserved successor name, reconciliation registers it `origin=reconciled` (bypassing the committed-origin guard), and compaction returns `reserved_conflict` and strands the expired frame (no hash collision); **[P1] loaded key material is not bound to this control-plane installation or epoch** — `compact_apply` calls `load_pseudonym_key(config, paths)` with default epoch and no `expected_key_id` and never binds `paths.state_root` to the control database, so a wrong/cross-installation key produces valid-shaped but unattributable audit lineage (reproduced: key A→B); **[P1] commit-uncertain compaction retirement can re-adopt expired bytes** — after `_swap_ledger` deletes the old row, `_remove_old_file` only reports `uncertain` and compaction records no durable retirement intent (only retention got the tombstone), so a lost un-fsync'd unlink resurrects the old mixed file and reconciliation re-registers it live with its expired frame; **[P1] the integrated head implements a missing A07 by wedging legal upgrade state** — the reviewed base ends at A06 with no accepted A07, and the legacy-occupant guard aborts every spool acquisition for a legal pre-reservation `origin=committed` reserved row with no migration/recovery, and PR #79 depends on PR #78 which is open on `349150e` with its own exact-head FAIL. **Owner decisions (2026-08-01, via the round-3 forks): (Q1) wire full key-ID/epoch binding now (control-plane `_installation_key`, migration 11), and (Q2) auto-converge legacy reserved occupants by rehoming them (no operator step).** A07 is corrected to that contract (this ledger's A07 row, the ADR 0007 addendum, the plan §1 register and §4.7). **Disposition: neither round-2 head is accepted; both remediations are being reworked per the corrected A07 — PR #78 first (docs/A07/ledger, must land on protected `main` behind a fresh independent full-SHA PASS), then PR #79 rebased onto the corrected `main`; each remediation SHA lands only behind its own fresh independent full-SHA review with no P0/P1. W03 and G03 stay In progress.** No prior evidence is rewritten; the corrected reviews supersede the withdrawn round-2 claims |

Add each discovered issue with severity P0–P3, owner, affected gates,
reproduction/evidence, and disposition. Stop only the affected workstream when a
locked privacy guarantee is infeasible, donor ownership is uncertain, a safe
reversible migration cannot be designed, or a change would materially expand
external writes, hosted operation, raw-content handling, or multi-tenancy.

## Update procedure

For every package:

1. Set it to **In progress** only after its dependencies permit work.
2. Link the implementation commit, tests, docs, and gate command output.
3. Record all open defects and external evidence separately.
4. Have CI or an independent reviewer evaluate the exact candidate.
5. Mark **Passed** only when every gate clause is satisfied.
6. Update the highest-passed gate and current snapshot without rewriting prior
   evidence; retain superseded evidence as dated history or immutable links.

Engineering completion, release-candidate readiness, and release completion are
three distinct outcomes. G18 can produce a release evidence packet; it does not
authorize or claim publication.
