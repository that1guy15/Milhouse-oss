# G03 evidence — SQLite control plane, durable spool, replay, and retention

Status: **NOT ready and NOT accepted.** Protected `main` is unchanged at `c7390ed`; no head is passing
an independent review. The exact, artifact-linked review record lives in the status ledger
(D07-D10); this packet defers to it and only summarizes. **The reviews are distinct heads, not one tree:**
the **PR #76** exact-head review ([5151585903](https://github.com/that1guy15/Milhouse-oss/pull/76#issuecomment-5151585903),
2026-08-01, head `f9ce189`, tree `fb372e2` = the `91b97b0` squash, 149 tests) FAILED that exact tree
with **3 P1 (same-survivor/different-exporter collision still strands; keyed lineage optional/unwired →
NULL `_audit` resources; audit omits the mandated old/new content/file hashes) + 1 P2**; a **distinct
PR #74** review ([5148867847](https://github.com/that1guy15/Milhouse-oss/pull/74#issuecomment-5148867847),
2026-08-01, head `a2e71b7d`) of the physical-crash-procedure PR — **not a second review of the
`91b97b0` tree** — FAILED with **[P1] a retention crash re-adopts privacy-expired bytes** whose
retention source is unchanged in the merged code, so the finding applies. **PR #77 posted NO review;
its "each finding addressed / head under review 91b97b0" packet was a later overstatement, WITHDRAWN.**
The code remediation PR #79 was then independently reviewed on **four** distinct heads and FAILED
each: round 1 `1b19a91` (4 P1,
[5152583787](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5152583787)); round 2
`9f0947a` (4 P1,
[5154152313](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5154152313), D08(B));
round 3 `9c08351962ba2af0521cef41a6e07c7e83df798b` (2 P1,
[5157727285](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5157727285)); and round 4
`fde0cc6f5fac43da137e9a6eb39aef825623a325` (3 P1 + 1 P2,
[5159628362](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5159628362), D09).
The docs/A07 correction PR #78 also FAILED on head `349150e` (3 P1 + 1 P2,
[5154004969](https://github.com/that1guy15/Milhouse-oss/pull/78#issuecomment-5154004969), D08(A)) and
again on head `aac8fc92085a81d1069145e6b9dd6fb677b7d68b` (3 P1,
[5159451366](https://github.com/that1guy15/Milhouse-oss/pull/78#issuecomment-5159451366), D09), then
head `f9e67870a54daa20971065bce74448eddd16d364` (1 P1 + 1 P2,
[5160141657](https://github.com/that1guy15/Milhouse-oss/pull/78#issuecomment-5160141657), D10): the
field-presence checker accepted empty values and Markdown-styled declaration text. The P1 correction
requires one canonical declaration plus ordered, unique, non-empty bounded field values; the P2 stale
round-3 wording is tracked in [issue #81](https://github.com/that1guy15/Milhouse-oss/issues/81).
Owner-approved amendment **A07** remains the governing correction (control-plane key-ID/epoch
binding; auto-converging reserved-namespace remediation; compaction retirement tombstone). PR #78
must correct its latest FAIL and land first; PR #79 must then rebase and correct its latest FAIL.
**G03 and W03 stay In progress; each remediation SHA lands only behind its
own fresh independent full-SHA review with no P0/P1. All historical author-evidence below predates
the current D07-D10 inventory and is superseded by it.**

## Open findings from the independent review (2026-07-31, `b2822f3`)

- **[P1] Bounded collision probe still strands expired data** — `_resolve_successor` searches only
  a finite set of publicly predictable ids and then reports `collision_bound`, leaving the
  mixed-expiry source intact; occupying every candidate retains expired data past its privacy
  deadline. Correction: reserve the compaction successor namespace at batch-id ingress (or durably
  reserve an unpredictable successor) so no finite terminal collision can leave data beyond its
  deadline.
- **[P1] Token shape is not proof of keyed derivation** — `_keyed_resource` validates only the
  grammar of an overridable `fingerprint()` result, so a `Pseudonymizer` subclass/proxy can persist
  caller-controlled canonical-shaped input. Correction: derive inside a trusted, non-overridable
  boundary (require the exact concrete `Pseudonymizer`), retaining grammar validation as defense.
- **[P1] Compaction lineage cannot be deferred to W06 by an evidence note** — production compaction
  records NULL lineage resources; plan §4.7 makes keyed old/new lineage a locked W03 deliverable, and
  an evidence-document deferral does not move it. Deferring to W06 (downstream of G05, while W05
  depends on G03) also implies a dependency cycle. Correction: wire installation-keyed lineage into
  production W03 compaction, OR obtain an explicit owner-approved plan/ADR amendment resolving the
  ownership and the cycle — an owner decision, pending.
- **[P1] The ledger overstated the failed remediation** — implementation-status.md/D06 and this
  packet claimed the PR #72 findings were remediated and every assertion passing, but PR #72's
  exact head `9128fad` (= the merged tree) carries a qualifying independent FAIL with these same
  blockers and no later production commit addresses it. That language is withdrawn here and in the
  status ledger; W03/G03 return to **In progress**.
- **[P2] The packet is not bound to final exact-head evidence** — the previously recorded `4644
  passed` predated the added cases; the packet must be refreshed with the exact tested SHA/tree,
  environment, coverage totals, and effective replay count on the remediated head (done below).

## WITHDRAWN — the PR #76/#77 "remediation status" claims (DISPROVEN 2026-08-01)

**Every assertion in the PR #76/#77 packet "remediation status" is WITHDRAWN and DISPROVEN — do not
read it as current evidence.** It claimed each finding was addressed and that the head under review
was `91b97b0`. The **PR #76** exact-head review then FAILED that exact tree (see D07):
([5151585903](https://github.com/that1guy15/Milhouse-oss/pull/76#issuecomment-5151585903), head
`f9ce189`, tree `fb372e2` = the `91b97b0` squash) reproduced a same-survivor/different-exporter
collision that still strands, proved keyed lineage is optional/unwired (NULL `_audit` resources), and
found the plan-mandated old/new hashes still missing. A **distinct PR #74** review
([5148867847](https://github.com/that1guy15/Milhouse-oss/pull/74#issuecomment-5148867847), head
`a2e71b7d`) of the physical-crash-procedure PR — **NOT a second review of the `91b97b0` tree** — proved
the retention crash re-adopts privacy-expired bytes; its retention source is unchanged in the merged
code, so the finding applies. Specifically DISPROVEN:
"each finding is addressed"; "a foreign occupant of the derived id is reachable only by a SHA-256
collision" (a routine same-survivor pair with different exporters collides with NO SHA-256 collision);
"a maintenance caller loads the pseudonym key" (no production caller exists — production compaction
writes NULL lineage); and "the head under review is `91b97b0`" (that head FAILED). **The head under
review is NOT `91b97b0`.** The code-remediation PR #79 was independently reviewed on four heads and
FAILED each — round-1 head `1b19a91` with four P1s
([5152583787](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5152583787)) and round-2
head `9f0947a` with four P1s ([5154152313](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5154152313),
D08(B)); round-3 head `9c08351962ba2af0521cef41a6e07c7e83df798b` with two P1s
([5157727285](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5157727285)); and
round-4 head `fde0cc6f5fac43da137e9a6eb39aef825623a325` with three P1s and one P2
([5159628362](https://github.com/that1guy15/Milhouse-oss/pull/79#issuecomment-5159628362), D09).
The docs/A07 correction PR #78 head `349150e` FAILED with 3 P1 + 1 P2
([5154004969](https://github.com/that1guy15/Milhouse-oss/pull/78#issuecomment-5154004969), D08(A)),
and its later head `aac8fc92085a81d1069145e6b9dd6fb677b7d68b` FAILED with three P1s
([5159451366](https://github.com/that1guy15/Milhouse-oss/pull/78#issuecomment-5159451366), D09).
The correction proceeds per A07, landing PR #78 first and then rebasing PR #79; each future candidate
must receive its own exact-head review. See D07, D08, D09, and A07 for the artifact-linked record.

The reference material below predates D07 and is likewise superseded and disproven by it; it is
retained only as dated historical author-evidence, not as current remediation status.
This packet certifies only the W03 scope (the durable spool, SQLite control state,
replay, retention, and the crash/concurrency/failure-injection harness) and claims no runtime,
release, or later-work-package readiness.

W03 was delivered incrementally on top of the accepted G02 base (`90f8e4b`): the secured SQLite
control plane, ordered migrations, fenced leases, and the global commit barrier (slices 1–3); the
self-describing spool segment wire, atomic durable ledger commit, mandatory reconciliation, and
quarantine/recovery (slices 3–3d); the structured-log file surface deferred from G02 by amendment
A04 (slice 3d); source cursors, per-rule/version derivation checkpoints, the exporter delivery
protocol, and deterministic idempotent replay (slice 4); `spool verify`, the `_audit` trail,
retention preview/apply, and the secure no-follow removal primitive (slice 5a/5b); audited
restartable compaction of mixed-expiry segments (slice 5c); and the crash/concurrency/
failure-injection harness and this evidence packet (slice 7). The D04 and D05 defect remediations and
the slice-5c review remediation are recorded in the durable ledger and were each corrected before or
on the merges they bind to.

## Local candidate evidence — 2026-07-31

Environment: macOS 26.5.2 (`25F84`), arm64, GNU Make 3.81, uv 0.11.29, CPython 3.14.6. Gate targets
run through `./scripts/run_make.py TARGET`, which strips inherited Make and shell controls before
Make parses the repository file.

| G03 assertion | Canonical evidence | Result |
|---|---|---|
| A kill before the atomic rename exposes no partial batch | `tests/unit/test_g03_failure_injection.py::test_a_kill_before_rename_exposes_no_partial_batch` (spool commit path), plus `tests/unit/test_config_filesystem.py` (`_rename_no_replace` refuse → `WRITE_FAILED`, and `COMMIT_UNCERTAIN`) and `tests/unit/test_spool_quarantine.py` (a stale staged temporary is quarantined, never a committed batch) | Passed; a rename failure publishes no `batch-*.jsonl` and no ledger row, a lingering staged temporary is recovered by reconciliation, and the batch is never acknowledged |
| A kill after commit loses no acknowledged record | `tests/unit/test_g03_failure_injection.py::test_a_kill_after_commit_loses_no_acknowledged_record` (restart reconciles; row, file, and replayed ids intact), plus `tests/unit/test_spool_commit.py` (durability fsyncs gate acknowledgement; ledger failure → `MH_SPOOL_COMMIT` commit-uncertain) and `tests/unit/test_spool_reconcile.py` (published-but-unrecorded orphan reconciles back into the ledger) | Passed; every acknowledged record survives a restart and no ledger row is ever left without its durable file |
| Concurrent writers produce valid non-interleaved segments | `tests/unit/test_g03_concurrent_writers.py` (four independent writers, each its own connection and barrier instance, commit forty-eight segments concurrently), plus `tests/unit/test_state_barrier.py` (cross-process readers-writer semantics) and `tests/unit/test_spool_reconcile.py` (a second writer acquisition waits for the exclusive barrier) | Passed; every segment lands exactly once as its own atomic file with a distinct batch id and frames belonging only to it — none lost, duplicated, or interleaved |
| Corrupt tails preserve valid frames and are quarantined safely | `tests/unit/test_spool_quarantine.py` (corrupt orphan/committed/mismatched files quarantined with a safe reason, plus crash-during-quarantine convergence) and `tests/unit/test_spool_reader.py` (a torn trailing frame fails closed as `MH_SPOOL_TRUNCATED`) | Passed; a corrupt file is quarantined with fixed metadata and valid committed segments are never dropped |
| Replaying 10,000 records twice yields identical logical IDs and counts | `tests/unit/test_g03_replay_scale.py` (commit 10,000 records, replay twice) | Passed; both passes return 10,000 distinct record ids in identical order, and the second pass re-emits them with `already_delivered` idempotent delivery (count overridable via `MILHOUSE_G03_SCALE_RECORDS` only to shrink for local iteration; the default is the gate-literal 10,000) |
| Files/directories are `0600`/`0700` where supported | `tests/unit/test_spool_commit.py` (segment files `0600`, day directories `0700`, failed stage mode 0), `tests/unit/test_spool_reconcile.py`, `tests/unit/test_spool_quarantine.py`, and `tests/unit/test_log_store.py` | Passed; owner-only `0600` files under owned ACL-free `0700` directories, enforced no-follow with fixed errors on any deviation |
| Pending data is never pruned before its record-class privacy expiry; intentional audited expiry is detected and not reported as accidental loss | `tests/unit/test_spool_retention_preview.py`, `tests/unit/test_spool_retention_apply.py` (per-record `expires_at` drives pruning; `pruned_undelivered` critical audit; crash-during-prune convergence), and the delivery hard-expiry gate in `tests/unit/test_spool_exporter.py` | Passed; a backlogged segment is retained until its record-class expiry, an expired-undelivered prune is a distinct audited critical event, and expired records are withheld from egress |
| The structured-log file surface persists, rotates, recovers every crash boundary, refuses malformed/conflicting segments, enforces access, deletes only expired closed segments, and a logging failure never changes acknowledgement | `tests/unit/test_log_store.py`, `tests/unit/test_log_active_retention.py` (crash at every rotate boundary), `tests/unit/test_log_remediation.py`, `tests/unit/test_log_d04.py`, `tests/unit/test_log_sink.py` | Passed; the concrete structured-log file surface deferred from G02 by amendment A04 is complete and crash-recoverable, and a logging failure emits only fixed `MH_LOG_*` metadata without changing record acknowledgement |
| Failure injection covers temporary write, file fsync, rename, directory fsync, ledger insert/commit, cursor update, derivation/checkpoint, export, destination confirmation, and exporter checkpoint | `tests/unit/test_g03_failure_injection.py` (cursor-update, derivation-checkpoint, and exporter-checkpoint faults via the shared SQL-fault harness; kill-before-rename) plus the referenced config-layer (temp write, file/dir fsync, rename), commit (ledger insert/commit), and quarantine suites | Passed; every enumerated durable-write boundary fails closed with a fixed code and no partial state — a cursor/checkpoint fault rolls back atomically, and an exporter-checkpoint fault between confirmation and the compare-and-set leaves delivery pending (at-least-once) |
| Security-critical modules reach at least 95% branch coverage | `./scripts/run_make.py test-coverage` (`--critical-branch 95`) | Passed on candidate head `e452756`; `line=97.96% branch=96.91%` overall with all 57 critical files at or above the 95% branch floor (`compaction.py`, `commit.py`, `segment.py`, `audit.py` included) |
| Complete offline topology | `./scripts/run_make.py test` | Passed on candidate head `e452756` (CPython 3.14.6 / macOS 26.5.2); 4661 passed and one privileged bind-mount capability test skipped on macOS |
| Documentation, workflow, and skill validation | `./scripts/run_make.py quality`, `docs-check`, `skill-check` | Passed; formatting, lint, strict typing, and repository/workflow/skill/documentation checks pass |

## The crash/concurrency/failure-injection harness (slice-7 deliverable)

`tests/unit/_g03_harness.py` is the shared harness the `test_g03_*` suites reuse: a spool builder, a
deterministic record/segment generator, independent-writer factories (own connection and barrier
instance per thread), and a fault-injecting SQLite connection proxy (`FaultyConnection` /
`inject_sql_fault`) that forces a chosen durable-write statement to fail so a boundary's atomic
rollback and fixed-code fail-closed behaviour can be exercised deterministically without a real
crash. It consolidates the previously per-file `monkeypatch os.fsync/replace/write` idioms into one
reusable vocabulary and hosts the net-new 10,000-record scale and concurrent-writer proofs.

## Pending / deferred

- **Independent full-SHA review** of the slice-7 candidate head, and of the W03 packages, per the
  acceptance discipline recorded in the ledger.
- **Supported-host physical-crash evidence.** The harness proves the crash boundaries deterministically
  by in-process fault injection and restart-by-reconciliation; the plan additionally records real
  power-loss / `SIGKILL` evidence on a supported host as owner-provided acceptance evidence (E06), not
  reproducible in CI.
- **Compaction audit lineage — the earlier "wired into W03" and "load a file for provenance" claims
  are both DISPROVEN (see D07, D08).** The PR #76 packet's threaded-`Pseudonymizer` claim was unwired
  (NULL `_audit` lineage), and the round-1/round-2 PR #79 attempts that merely loaded a key at the
  bound path were caller-forgeable (a wrong/cross-installation key passes). Per the corrected amendment
  A07, W03 records the installation's non-secret key ID/epoch in the control plane (migration 11), and
  compaction binds its state root to the control database, reads that record, and loads with the
  recorded `expected_key_id`/epoch, failing closed before any mutation on an absent/unprovisioned,
  missing, or mismatched key. The fix is pending on the round-3 PR #79 rework.

## Gate conclusion

**WITHDRAWN / DISPROVEN — see the Status section at the top of this packet and the status ledger
(D07, D08).** The prior conclusion asserted the remediation had merged and that G03 was "READY FOR an
independent full-SHA review of `91b97b0`." The **PR #76 exact-head** review then FAILED that exact tree
(D07); a **distinct PR #74** review (head `a2e71b7d`, not a second review of the `91b97b0` tree) proved
a retention crash re-adoption that applies to the unchanged retention source. The code remediation
PR #79 was subsequently reviewed on four heads and **FAILED each** — `1b19a91`, `9f0947a`,
`9c08351962ba2af0521cef41a6e07c7e83df798b`, and
`fde0cc6f5fac43da137e9a6eb39aef825623a325` (D08/D09). The docs/A07 correction PR #78 also **FAILED**
on `349150e` and `aac8fc92085a81d1069145e6b9dd6fb677b7d68b` (D08/D09); no head is passing.
W03 and G03 are **In progress**. PR #78 must correct its current FAIL and land first; PR #79 must
then rebase and correct its current FAIL. Acceptance requires a fresh
independent full-SHA PASS on each future remediated production-`main` SHA (no P0/P1), E06 supported-host
physical-crash evidence, and owner acceptance, recorded then against that SHA.
