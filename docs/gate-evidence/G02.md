# G02 evidence — domain, configuration, identity, trust, and privacy

Status: **candidate — evidence assembled; G02 acceptance is owner-pending.** This record does not
mark G02 passed. Only an explicit owner acceptance tied to an exact commit, with an independent
report-only review and no unresolved P0/P1, passes the gate and unblocks W03 and W04.

This packet's integrated candidate is its own pull-request head, whose hosted Required CI validates its
exact reviewed tree and which carries the owner-approved plan amendment A04 / defect-D02 gate-scope
correction. `fad147973f6ec3e65bfef7d7b01566d7dc75cdc4` is the runtime-plus-coverage base: it holds the
complete, hardened, and schema-locked `local_log` implementation (PR #33 — header, trailer, and segment
digest; hostile-`tzinfo`/iterable failure normalization; and owner-approved amendment A05, the exact v1
stored schema) together with the 36-file critical-coverage enforcement (PR #31). The local command
results below were run on the packet tree, which equals `fad1479` plus this packet's documentation and
the strengthened `tests/unit/test_gate_scope.py`; the A04 correction changes only gate-scope
documentation, no source or coverage-critical code. Owner acceptance of G02 must bind to the exact
protected-main commit that merges this packet; a follow-up status entry records that merge SHA,
squash-tree equality, post-merge Required CI, and the acceptance.

W02 was delivered incrementally: the domain, configuration, identity, and privacy foundations landed in
merged PRs #8-#21; owner-approved amendments A02 (persisted local structured-log contract, ADR 0016) and
A03 (D01 historical DCO disposition, ADR 0017) were ratified in PR #26; the W02-owned `local_log` event
wire, encoder, privacy authorization, golden vectors, and sink interface landed in PRs #27-#31; and the
header, trailer, segment digest, hostile-failure hardening, and the exact v1 schema (amendment A05)
completed the wire in PR #33.

Per owner-approved plan amendment A04 (2026-07-23, defect D02), G02 owns only the W02 evidence below and
certifies the W02 redaction, rendering, structured-error, structured-event, and `local_log` wire and
stream-sink primitives. The dependency-blocked concrete surfaces are validated at their owning gate per
amendment A04 and the work-package ownership split: the structured-log file surface at G03 (W03), the
CLI/stderr binding and diagnostics bundle at G06 (W06), the generated-report surface at G09 (W09), and
backup/restore/target-and-full purge at G16 (W16). G02 neither requires nor certifies those concrete surfaces; requiring them would be a gate
cycle because each owning gate transitively depends on G02, a property locked by
`tests/unit/test_gate_scope.py`.

## Local candidate evidence — 2026-07-23

Environment: macOS 26.5.2 (`25F84`), arm64, GNU Make 3.81, exact uv 0.11.29, CPython 3.14.6. Gate
targets run through `./scripts/run_make.py TARGET`, which strips inherited Make and shell controls
before Make parses the repository file.

| G02 assertion | Canonical evidence | Result |
|---|---|---|
| Every example validates and unknown keys fail clearly | `./scripts/run_make.py quality` (runs `validate_config.py`) plus `tests/contract/test_config_examples.py`, `tests/unit/test_config_models.py`, `tests/unit/test_config_loader.py`, `tests/unit/test_cli_config.py` | Passed; the four public example TOML files validate, strict no-coercion models reject unknown/extra keys with clear coded errors, and `config validate` / `config schema` exercise the offline path |
| Identical identity input yields identical IDs across processes and platforms | `tests/integration/test_identity_subprocess_integration.py`, `tests/contract/test_identity_portability_contract.py`, `tests/unit/test_identity.py`, fixture `tests/fixtures/w02/identity-portability-v1.json` | Passed; an isolated `-s -P` child recomputes record IDs, dedupe keys, content hashes, and canonical envelopes byte-identically across varied `PYTHONHASHSEED`, locale, timezone, and object order, and the hosted CI matrix re-locks the fixture on Ubuntu/macOS across Python 3.11-3.14 |
| Secret values never appear in the W02 redaction/rendering/structured-error/structured-event primitives, canonical records, the W02 config-command output, or exceptions and tracebacks (the concrete generated-report surface is G09) | `tests/security/` boundary suite: `test_privacy_boundary.py`, `test_egress_boundary.py`, `test_layered_privacy_boundary.py`, `test_pseudonym_key_boundary.py`, `test_structured_logging_boundary.py`, `test_log_sink_boundary.py`, `test_log_framing_boundary.py`, `test_config_secret_boundary.py`, `test_domain_validation_boundary.py` | Passed; planted secret/PII/path/markup/prompt-injection canaries are absent from records, structured events, error graphs, tracebacks, the W02 config-command output, and the log-wire and stream-sink output; failures retain only fixed coded metadata and keyed fingerprints |
| The `local_log` wire and stream-sink output are byte-stable across supported platforms and never carry secrets, PII, paths, prompts, transcripts, or tool output in the projected bytes, the stream-sink output, exceptions, or tracebacks (the concrete structured-log file surface is G03 and the concrete CLI/stderr binding is G06) | `tests/unit/test_log_wire.py`, `tests/unit/test_log_framing.py`, `tests/unit/test_log_schema_contract.py`, `tests/property/test_log_wire_properties.py`, `tests/integration/test_log_wire_subprocess_integration.py`, `tests/unit/test_log_sink.py`, `tests/security/test_log_sink_boundary.py`, `tests/security/test_log_framing_boundary.py` | Passed; the full header/event/trailer segment is byte-identical in-process and across three isolated subprocesses (varied hash seed/locale/timezone) on Ubuntu and macOS, the exact v1 schema is locked to normative vectors, the adversarial canary matrix — including hostile `tzinfo`, `__iter__`, and `__next__` collaborators — never reaches the wire, exceptions, tracebacks, or child stderr, the `local_log` egress guard fails closed, and the stream sink normalizes hostile failures and rejects partial writes |
| Property tests cover nested, encoded, Unicode, multiline, Markdown/HTML, URL, path, PII, and prompt-injection cases | `tests/property/` (11 modules): canonical, clock, config path/secret, config plugin, egress policy, log wire, privacy, privacy redaction, pseudonym key, resource manifest, structured logging | Passed; Hypothesis and parametrized corpora drive the nine required adversarial content shapes through the redaction and log-wire property suites, while the canonical, clock, config path/secret, config plugin, egress-policy, pseudonym-key, privacy, resource-manifest, and structured-logging suites cover their own invariants |
| Security-critical modules reach at least 95% branch coverage | `./scripts/run_make.py test-coverage` (`--critical-branch 95`) | Passed; `line=98.25% branch=97.18%` overall with all 36 critical files at or above 95% branch coverage, and `core/log_wire.py` at 98% branch (99% line) after the header, trailer, digest, and hostile-failure paths |
| Complete offline topology | `./scripts/run_make.py test` | 3769 collected; 3768 passed and one privileged bind-mount capability test skipped on macOS |
| Repository privacy and secrets | `./scripts/run_make.py secret-scan` and `secret-scan-self-test` | Passed; the tracked tree and full history scan clean, and both runtime-planted current-tree and committed-history values are detected |
| Documentation, workflow, and skill validation | `./scripts/run_make.py quality`, `docs-check`, `skill-check` | Passed; formatting, lint, strict typing, repository/workflow/skill validation, and 66-file/202-link/49-external-probe documentation checks pass |

The 36 critical files comprise the 22 `src/milhouse` W02 modules (config models/loader/paths/plugins/
secrets/filesystem; core canonical/clock/errors/logging/log-wire; domain validation/identity/records;
privacy egress/keys/allowlist/pseudonym/redact/render/sanitize; resources) and the 14 W01 tooling
scripts, each held at or above the 95% branch floor by `check_coverage.py`. The enforced critical set
already includes `src/milhouse/core/log_wire.py` (added at PR #31); PR #33 held it at 98% branch
(99% line) after adding the header, trailer, segment digest, and hostile-failure normalization paths.

## Contained child-process disclosure oracle

G01 recorded that W02 privacy tests must add their own contained child-process output oracle before
generating additional sensitive-shaped values. The identity and log-wire subprocess integration tests
satisfy this: each spawns an isolated interpreter, bounds child stdout/stderr, asserts an empty
stderr and a byte-exact stdout, and fails closed (`SystemExit(70)`) rather than emitting a traceback,
so a planted value can only be observed through the asserted, bounded channel. The log-framing boundary
suite adds a failing-path oracle: `tests/security/test_log_framing_boundary.py` runs isolated children
that let a hostile `tzinfo`/iterable failure propagate uncaught and asserts the planted canary never
reaches child stderr even in an unhandled traceback.

## Hosted evidence

Every W02 change merged to protected `main` only after its own green Required CI run and independent
report-only review:

- Foundations PRs #8-#20 (config, core, domain, privacy, CLI) and PR #21 (identity portability) each
  passed the hosted `required-ci` aggregate — including Python 3.11-3.14 compatibility, macOS/Python
  3.11 and 3.14 identity portability, coverage, gitleaks, CodeQL, and DCO — before protected merge.
- PR #26 (A02/A03 ratification) passed all 20 checks; protected merge `54c240d`.
- PR #27 (`local_log` egress surface) `b66abf6`, PR #28 (CanonicalJSONV1 event wire) `b296682`, PR #29
  (golden/determinism corpus) `e89ca0c`, and PR #30 (stream sink) `bb0571d` each passed all 20 hosted
  checks with exact-tree-verified DCO-signed protected merges and green post-merge Required CI. PR #30
  additionally resolved two owner-review P1 findings (hostile-failure normalization and complete-write
  enforcement), re-passing hosted CI and an independent re-review before merge.
- PR #33 (header/trailer/digest, hostile-failure hardening, amendment A05) passed all 20 hosted checks
  — including both macOS `identity-portability` jobs, which now run the log-wire subprocess determinism
  test — with an exact-tree-verified DCO-signed protected merge `fad1479` and green post-merge Required
  CI. It resolved a four-finding owner review (two P1s: hostile-failure disclosure and the unamended
  locked schema; one P2: the retention bound; and the macOS portability gap).

Per-slice provenance for the A02/A03 ratification, `local_log` implementation, and schema completion:

| Slice | Reviewed head | Protected merge / tree | Post-merge Required CI | Independent review |
|---|---|---|---|---|
| PR #26 A02/A03 ratification | `fb4b282` | `54c240d` / `977b5d6` | run `29946022658` success | report-only 3-lens gate-review, verdict pass (1 P1 + 2 P3 resolved pre-merge) |
| PR #27 `local_log` egress surface | `2a69185` | `b66abf6` / `5fcc54f` | run `29955284240` success | report-only gate-review, verdict pass |
| PR #28 CanonicalJSONV1 event wire | `d7db338` | `b296682` / `ec3dc5f` | run `29969708853` success | report-only gate-review, verdict pass |
| PR #29 golden/determinism corpus | `63f21c3` | `e89ca0c` / `a34e590` | run `30008093665` success | report-only 2-lens gate-review, verdict pass (2 P2 + 1 P3 resolved pre-merge) |
| PR #30 stream sink | `acabb18` | `bb0571d` / `c07f945` | run `30014858708` success | report-only pass, then owner [Codex review](https://github.com/that1guy15/Milhouse-oss/pull/30) — 2 P1s resolved and independently re-reviewed pass |
| PR #31 G02 packet + coverage enforcement | `98920fe` | `b56b698` / `03b90f4` | run `30018141291` success | owner [Codex review](https://github.com/that1guy15/Milhouse-oss/pull/31) — 2 P1 + 1 P2, addressed by amendment A04 and PR #33 |
| PR #33 header/trailer/digest, hardening, amendment A05 | `213d1ea` | `fad1479` / `3140bec` | run `30053041878` success | report-only gate-review verdict pass-with-nits, then owner [Codex review](https://github.com/that1guy15/Milhouse-oss/pull/33) — four findings resolved before merge |
| This packet — A04 / D02 gate-scope correction | this PR head | pending merge | pending | supersedes the withdrawn PR #32; independent report-only review recorded on this PR before merge |

The report-only gate reviews were independent Claude Code session reviews that found no unresolved
P0/P1 at merge and carry no separate GitHub review URL; the owner Codex reviews on PRs #30, #31, and #33
are recorded in the linked pull requests. Each merged tree equals its reviewed-head tree (exact-tree
squash), and each post-merge Required CI run validated the merged tree.

## Gate conclusion

Evidence assembled; **G02 acceptance is owner-pending.** The re-scoped W02-owned G02 assertions
(owner-approved plan amendment A04) each have passing local and hosted evidence on the `fad1479`
runtime base, coverage meets the 95% critical-branch floor across the 36 enforced files, and every
merged W02 change carried an independent report-only review with no unresolved P0/P1. This record does
not mark G02 passed and claims no runtime, release, or later-work-package readiness. W03 and W04 remain
dependency-blocked until the owner accepts G02 against this packet's exact protected merge commit.
