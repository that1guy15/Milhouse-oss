# G02 evidence — domain, configuration, identity, trust, and privacy

Status: **candidate — evidence assembled; G02 acceptance is owner-pending.** This record does not
mark G02 passed. Only an explicit owner acceptance tied to an exact commit, with an independent
report-only review and no unresolved P0/P1, passes the gate and unblocks W03 and W04.

This packet assembles the W02 evidence at protected-main commit
`bb0571d291d52f7df1d2b4266fb2253bf669f91b`. W02 was delivered incrementally: the domain,
configuration, identity, and privacy foundations landed in merged PRs #8-#21; owner-approved
amendments A02 (persisted local structured-log contract, ADR 0016) and A03 (D01 historical DCO
disposition, ADR 0017) were ratified in PR #26; and the W02-owned `local_log` wire, encoder, privacy
authorization, golden vectors, and sink interface landed in PRs #27-#30.

## Local candidate evidence — 2026-07-23

Environment: macOS 26.5.2 (`25F84`), arm64, GNU Make 3.81, exact uv 0.11.29, CPython 3.14.6. Gate
targets run through `./scripts/run_make.py TARGET`, which strips inherited Make and shell controls
before Make parses the repository file.

| G02 assertion | Canonical evidence | Result |
|---|---|---|
| Every example validates and unknown keys fail clearly | `./scripts/run_make.py quality` (runs `validate_config.py`) plus `tests/contract/test_config_examples.py`, `tests/unit/test_config_models.py`, `tests/unit/test_config_loader.py`, `tests/unit/test_cli_config.py` | Passed; the four public example TOML files validate, strict no-coercion models reject unknown/extra keys with clear coded errors, and `config validate` / `config schema` exercise the offline path |
| Identical identity input yields identical IDs across processes and platforms | `tests/integration/test_identity_subprocess_integration.py`, `tests/contract/test_identity_portability_contract.py`, `tests/unit/test_identity.py`, fixture `tests/fixtures/w02/identity-portability-v1.json` | Passed; an isolated `-s -P` child recomputes record IDs, dedupe keys, content hashes, and canonical envelopes byte-identically across varied `PYTHONHASHSEED`, locale, timezone, and object order, and the hosted CI matrix re-locks the fixture on Ubuntu/macOS across Python 3.11-3.14 |
| Secret values never appear in exceptions, logs, CLI, records, reports, or diagnostics | `tests/security/` boundary suite: `test_privacy_boundary.py`, `test_egress_boundary.py`, `test_layered_privacy_boundary.py`, `test_pseudonym_key_boundary.py`, `test_structured_logging_boundary.py`, `test_log_sink_boundary.py`, `test_config_secret_boundary.py`, `test_domain_validation_boundary.py` | Passed; planted secret/PII/path/markup/prompt-injection canaries are absent from records, structured events, error graphs, tracebacks, CLI/diagnostic output, and the stored/stderr log wire; failures retain only fixed coded metadata and keyed fingerprints |
| The `local_log` wire (section 4.15, A02) is byte-stable across supported platforms and never emits secrets/PII/paths/prompts/transcripts/tool output to files, stderr, exceptions, or tracebacks | `tests/unit/test_log_wire.py`, `tests/property/test_log_wire_properties.py`, `tests/integration/test_log_wire_subprocess_integration.py`, `tests/unit/test_log_sink.py`, `tests/security/test_log_sink_boundary.py` | Passed; a hand-verifiable golden line is byte-identical in-process and across three isolated subprocesses (varied hash seed/locale/timezone), the adversarial canary matrix never reaches the wire, the `local_log` egress guard fails closed, and the stream sink normalizes hostile failures and rejects partial writes |
| Property tests cover nested, encoded, Unicode, multiline, Markdown/HTML, URL, path, PII, and prompt-injection cases | `tests/property/` (11 modules): canonical, clock, config path/secret, config plugin, egress policy, log wire, privacy, privacy redaction, pseudonym key, resource manifest, structured logging | Passed; Hypothesis and parametrized corpora drive the required adversarial categories through redaction, egress, identity, and the log wire |
| Security-critical modules reach at least 95% branch coverage | `./scripts/run_make.py test-coverage` (`--critical-branch 95`) | Passed; `line=98.25% branch=97.19%` overall with all 36 critical files at or above 95% branch coverage |
| Complete offline topology | `./scripts/run_make.py test` | 3723 collected; 3722 passed and one privileged bind-mount capability test skipped on macOS |
| Repository privacy and secrets | `./scripts/run_make.py secret-scan` and `secret-scan-self-test` | Passed; the tracked tree and full history scan clean, and both runtime-planted current-tree and committed-history values are detected |
| Documentation, workflow, and skill validation | `./scripts/run_make.py quality`, `docs-check`, `skill-check` | Passed; formatting, lint, strict typing, repository/workflow/skill validation, and 65-file/196-link/46-external-probe documentation checks pass |

The 36 critical files comprise the 22 `src/milhouse` W02 modules (config models/loader/paths/plugins/
secrets/filesystem; core canonical/clock/errors/logging/log-wire; domain validation/identity/records;
privacy egress/keys/allowlist/pseudonym/redact/render/sanitize; resources) and the 14 W01 tooling
scripts, each held at or above the 95% branch floor by `check_coverage.py`. This packet adds
`src/milhouse/core/log_wire.py` — the A02 encoder and stream sink — to the enforced critical set so the
newest security-critical module carries the same self-enforcing 95% branch floor.

## Contained child-process disclosure oracle

G01 recorded that W02 privacy tests must add their own contained child-process output oracle before
generating additional sensitive-shaped values. The identity and log-wire subprocess integration tests
satisfy this: each spawns an isolated interpreter, bounds child stdout/stderr, asserts an empty
stderr and a byte-exact stdout, and fails closed (`SystemExit(70)`) rather than emitting a traceback,
so a planted value can only be observed through the asserted, bounded channel.

## Hosted evidence

Every W02 change merged to protected `main` only after its own green Required CI run and independent
report-only review:

- Foundations PRs #8-#20 (config, core, domain, privacy, CLI) and PR #21 (identity portability) each
  passed the hosted `required-ci` aggregate — including Python 3.11-3.14 compatibility, macOS/Python
  3.11 and 3.14 identity portability, coverage, gitleaks, CodeQL, and DCO — before protected merge.
- PR #26 (A02/A03 ratification) passed all 20 checks; protected merge `54c240d`.
- PR #27 (`local_log` egress surface) `b66abf6`, PR #28 (CanonicalJSONV1 wire) `b296682`, PR #29
  (golden/determinism corpus) `e89ca0c`, and PR #30 (stream sink) `bb0571d` each passed all 20 hosted
  checks with exact-tree-verified DCO-signed protected merges and green post-merge Required CI. PR #30
  additionally resolved two owner-review P1 findings (hostile-failure normalization and complete-write
  enforcement), re-passing hosted CI and an independent re-review before merge.

The D01 PR #21 squash DCO incident is separately recorded in [PR21-DCO.md](PR21-DCO.md); its permanent
historical disposition is owner-approved amendment A03 (ADR 0017), and strict DCO enforcement continues
unchanged over every later range.

## Gate conclusion

Evidence assembled; **G02 acceptance is owner-pending.** The six locked G02 assertions each have
passing local and hosted evidence at `bb0571d`, coverage meets the 95% critical-branch floor, and
every merged W02 change carried an independent report-only review with no unresolved P0/P1. This
record does not mark G02 passed and claims no runtime, release, or later-work-package readiness. W03
and W04 remain dependency-blocked until the owner accepts G02 against this exact commit.
