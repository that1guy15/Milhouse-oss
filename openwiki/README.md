# OpenWiki Output

This directory is reserved for generated or curated OpenWiki documentation.

Do not generate wiki content from private overlays, telemetry, raw agent sessions, `.env`, `spool/`, `data/`, `logs/`, or generated reports.

Suggested future command:

```bash
openwiki --init
openwiki generate
```
